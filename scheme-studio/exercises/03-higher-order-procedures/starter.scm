;;; Exercise 03: higher-order procedures

(define (keep predicate items)
  (error "TODO: retain the items for which PREDICATE is true"))

(define (compose outer inner)
  (error "TODO: return a procedure that applies INNER, then OUTER"))
