;;; Exercise 01: values and procedures

(define (square number)
  (error "TODO: return NUMBER multiplied by itself"))

(define (sum-of-squares left right)
  (error "TODO: add the squares of LEFT and RIGHT"))
