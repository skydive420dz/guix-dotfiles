;;; Exercise 02: list recursion

(define (square number)
  (* number number))

(define (sum-list numbers)
  (error "TODO: recursively add the elements of NUMBERS"))

(define (squares numbers)
  (error "TODO: recursively square the elements of NUMBERS"))
