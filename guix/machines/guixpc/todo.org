#+title: guixpc todo
#+created: 2026-07-03

* DONE Machine setup first
:PROPERTIES:
:created: 2026-07-03
:completed: 2026-07-10
:END:
Basic machine setup is stable enough to stop blocking normal polishing work.
Keep kernel optimization behind the separate hardware inventory task.

- [X] Confirm Wi-Fi survives reboot.
- [X] Confirm sleep policy remains disabled.
- [X] Add basic terminal utilities if missing, such as ncurses for clear/reset.
- [X] Decide base desktop/session direction: Emacs/EXWM.
- [X] Clean and repaste the machine after shutdown.

* DONE Display and EXWM startup stabilization
:PROPERTIES:
:created: 2026-07-11
:completed: 2026-07-11
:END:
Accepted baseline:

- Xorg starts on =HDMI-A-0= using the explicit =3440x1440_100= modeline.
- =xrandr= is fallback-only, not the normal path.
- =shell/xinitrc= starts wallpaper and Picom before fullscreen Emacs/EXWM.
- Emacs uses =-fs= and loads =~/.exwm= directly instead of using the Guix =exwm=
  wrapper's hardcoded =-mm= startup.
- Picom is scoped to Emacs opacity with =--detect-client-opacity=, both
  frame/client opacity-property exclusions, and =--daemon= initialization.

Rejected experiment:

- Do not add =amdgpu= to =initrd-modules= on this machine. It pulled AMDGPU/DRM
  into the initrd but made X fall back to a generic =default= 1024x768 screen.
  Reverting returned the accepted 3440x1440 state.

Remaining cosmetic issue:

- GRUB and earliest boot text are still not ultrawide. Diagnose manually with
  GRUB =videoinfo= before changing bootloader generation. If 3440x1440 is not
  listed there, Guix =gfxmode= cannot force it.

* DONE 2026-07-12 configuration audit remediation
:PROPERTIES:
:created: 2026-07-12
:completed: 2026-07-14
:END:

Canonical evidence, finding status, slice definitions, and acceptance gates:

[[file:../../../docs/audits/2026-07-12-full-configuration-audit.org][2026-07-12 full configuration audit and slice roadmap]]

The audit owns detailed status.  Keep this machine TODO as a low-noise queue so
the same checklist is not maintained in two places.

Completed remediation record:

- [X] Accept [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 7: Runtime convergence and SSH incident baseline][Slice 7: runtime convergence and SSH incident baseline]].  Elogind 255.24,
  converged System/Home runtime, and bounded command/PTY SSH cleanup passed.
- [X] Accept [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 8: Non-mutating validation harness][Slice 8: non-mutating validation harness]].  Live queries are read-only; batch state
  is isolated and write-guarded; independent failures and 13 ERT checks pass.
- [X] Accept [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 9: Command, path, formatter, and activation correctness][Slice 9: command, path, formatter, and activation correctness]].  Shells share one
  revision-reporting Guix wrapper; declaration and project links fail safely;
  JSONC, formatter discovery, lockfiles, project discovery, and shell lint pass.
- [X] Accept [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 10: One session-bus and converged desktop services][Slice 10: one session-bus and converged desktop services]].
  - [X] Build source commit =1321f76= and system candidate
    =/gnu/store/yr6x7ssaiwpw48891n7aqbpaa8jrqim1-system=.
  - [X] Push the checkpoint, run =gsr=, and reboot into System generation 82
    and Home generation 32.
  - [X] Pass both desktop-session checker modes, audible Bose Bluetooth output,
    both physical volume keys, and a real EXWM Kitty launch on the canonical bus.
    Audible HDMI is not required because this workstation does not use it.
- [X] Accept [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 11: EXWM startup, reload, and client-placement reliability][Slice 11: EXWM startup, reload, and client-placement reliability]].
  - [X] Build implementation commit =39df7c3= and Home candidate
    =/gnu/store/wg9jd7f1xp3hfc59x90krxq1dzxf9db3-home=.
  - [X] Pass strict Fish recovery, shell, live EXWM, 25/25 isolated ERT,
    placement concurrency, and cleanup gates.  The pre-activation strict
    desktop-session run passed with Bose connected; after reboot Bose was off,
    so the non-strict run passed with Bluetooth skipped.  Slice 10 retains the
    accepted audible Bluetooth and physical-volume-key evidence.
  - [X] Push the checkpoint, run =ghr=, and reboot into a fresh tty1/EXWM
    session so the store-copied Fish configuration is active.
  - [X] Accept fresh-session logs, reload/Picom, application placement, and
    keyboard/master-stack behavior.  System/Home generations are 83/33; the
    System store realization is unchanged from Slice 10.
- [X] Complete [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 12: Reproducible channels, build gates, and retention][Slice 12: reproducible channels, build gates, and retention]].
  - [X] Publish and clean-clone authenticate =sk-guix= introduction
    =eaade368=, target =b84ff24=, and orphan keyring =9122748=.
  - [X] Pin Guix =a8391f2=, Nonguix =3b66965=, and =sk-guix= =b84ff24=;
    pass the isolated authenticated time-machine provenance check.
  - [X] Pass the promoted package, quarantined Fish evaluation, Home, System,
    reviewed EXWM 0.35, shell, and fixture gates without activation.
  - [X] Pass live retention verification and the read-only rollback drill.
    The accepted next-promotion pins are System 84/83/81/80/75, Home
    34/33/32/29, and pull 5/4/3/2.
  - [X] Push the checkpoint, pull the exact lock, run =gsr= and =ghr=, and
    reboot into Pull generation 5, System generation 84, and Home generation
    34; fresh shell, editor, desktop, provenance, retention, and rollback checks
    pass.
- [X] Complete [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 13: Package ownership and per-dialect environments][Slice 13: package ownership and per-dialect environments]].
  - [X] Record the 13-package-plus-Kitty recovery target and the 75-package
    Home application/editor/development profile with only Emacs deliberately
    duplicated in the explicit lists; the final Home package adds
    =fish-foreign-env= for forced SSH command environment loading.
  - [X] Add independently pinned core, JVM Clojure, and full Racket manifests
    plus pure-shell entrypoints and build/runtime/leakage gates.  The final
    isolated full gate builds Home candidate =7f2lqwx64ixl=, retains the exact
    System candidate, and passes every runtime and unique-runtime absence probe.
  - [X] Run the complete 14-link Home preflight before Guix; execute its
    blocker/dangling and no-reconfigure negative fixtures in =shell-check=.
  - [X] Push and activate additive checkpoint =c826d55= as Home generation 35,
    reboot, and accept Home Emacs, packages, applications, fonts, persistent
    editor tools, strict shell fixtures, and desktop services.  System 84
    remained current and booted; Home 34 is the immediate fallback.
  - [X] After additive Home acceptance, remove non-recovery packages from
    System in a separate source checkpoint.  The pinned full gate built
    =/gnu/store/6680dpbpy035vs0g3c2f4vkk0sdri4n1-system= and proved the exact
    realized =emacs/fish/guile= overlap plus recovery/Home artifacts.
  - [X] Push subtractive checkpoint =da231e4=, run =gsr= only, and accept fresh
    System 85 at =/gnu/store/m5znrpyizvkkz77nn5nc7swl1jfhp7mf-system= with
    Home 35 unchanged.  Strict shell/editor/ownership/session gates pass;
    System 84 remains the immediate rollback.
  - [X] Push and activate the forced-command Fish correction as Home generation
    36 at =/gnu/store/n0xnfq95gry4xrlqknvsv682fqid0zd7-home=, reboot, and prove
    bare SSH finds Home-owned ShellCheck/SBCL.
  - [X] Record final runtime generations and rotate exact retention pins to
    System 85, Home 36, and pull 5, with 84/35/4 as the older recovery tier;
    verification and the read-only rollback drill pass.
  - [X] Pass the checked installed-base configuration-bootstrap sequence.  Both
    fixture coverage and isolated GuixPC =prepare= acceptance pass without
    changing live System/Home/Pull generations; blank-disk provisioning remains
    explicitly outside this slice.
- [X] Complete [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 14: Core Lisp editing correctness and structural editing][Slice 14: core Lisp editing correctness and structural editing]].
  - [X] Build and push implementation checkpoint =448f1aa= with pinned Puni,
    Eshell highlighting, authored snippets, backend guards, explicit
    Geiser/SLY ownership, and candidate-profile gates.
  - [X] Activate Home candidate =693n0j3q7ikb= as generation 37 and reboot;
    keep current/booted System generation 85 unchanged.
  - [X] Pass strict ownership, live Home Emacs, 30/30 isolated ERT, structural
    editing, Guile PTY readline/color/completion, and backend acceptance.
  - [X] Rotate the exact Home retention root to generation 37 with generation
    36 as the immediate rollback; retain System 85 and pull 5.
- [X] Complete [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 15: Elisp, Guile, and Common Lisp project/test workflows][Slice 15: Elisp, Guile, and Common Lisp project/test workflows]].
  - [X] Implement real Elisp, Guile, and ASDF project fixtures plus the isolated
    project dispatcher, shared Lisp command/window surface, package-lint
    ownership, strict ASDF policy, and disposable connected-backend checker.
  - [X] Build and pass the complete candidate-profile gate on GuixPC, including
    project checks and asynchronous Geiser/SLY failure recovery.  Candidate
    results are =aghqh0si8x2p= Home and =6680dpbpy035= System.
  - [X] Push implementation checkpoint =4169217=, run =ghr=, and reboot into
    Home generation 38 at =/gnu/store/aghqh0si8x2pr0cmfyir7mgaks217yb6-home=.
    System generation 85 remains current and booted; the unchanged candidate
    System =6680dpbpy035= was built but never activated for this slice.
  - [X] Pass the fresh live Emacs probe, all 37 isolated ERT checks, Elisp,
    Guile, and Common Lisp project checks, strict shell validation, and
    disposable asynchronous Geiser/SLY backend acceptance.
  - [X] Rotate the exact Home retention root to generation 38 with generation
    37 as the immediate fallback; retain System 85 and pull 5.  Live retention
    verification and the read-only rollback drill pass.
- [X] Complete [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 16: Clojure/JVM vertical slice][Slice 16: Clojure/JVM vertical slice]].
  - [X] Implement explicit ownership: the JVM manifest supplies Guix OpenJDK,
    the Clojure JAR, clojure-lsp, clj-kondo, and cljfmt; Home supplies only
    emacs-clojure-mode, clj-kondo, and cljfmt; System supplies none of the
    Clojure/JVM stack.
  - [X] Implement the direct =java ... clojure.main= project wrapper and its
    generic classpath, Clojure, formatter, linter, REPL, and LSP primitives.
    Keep test/reload/check policy in the tracked fixture Makefile, keep
    =deps.edn= metadata-only, and provide a project-keyed comint REPL plus
    explicit lsp-mode/clojure-lsp startup.  Do not add CIDER or Maven-resolved
    nREPL middleware at the pinned channels.
  - [X] Pass the disposable JVM runtime/project checker and its format, lint,
    and test negative controls; build candidate Home =mhjlpha5ww42= and pass
    all 44 candidate batch ERT checks.  The unchanged System candidate remains
    =6680dpbpy035=; neither candidate is activated by this evidence.
  - [X] Pass the final complete candidate gate, including realized package and
    executable leakage checks plus the disposable connected REPL, Home
    cljfmt/Flycheck, and clojure-lsp checker through the production nested-shell
    path.  This gate is deliberately non-activating.
  - [X] Push implementation checkpoint =8c96541=.
  - [X] Run =ghr=, reboot, and pass fresh live Home/editor/project acceptance;
    verify no checker-owned process, tracked-source mutation, or Maven/
    dependency-HOME state remains.  The candidate gate must launch the
    connected editor checker directly with pinned Guix, while its real REPL and
    clojure-lsp processes use the production nested JVM-shell path.  Any
    ignored =.lsp/.cache= or =.clj-kondo/.cache= state must remain inside the
    disposable project copy; primary analysis, kondo, and log state must remain
    project-keyed under isolated XDG state.  Then rotate the exact Home
    retention root.  Home generation 39 at
    =/gnu/store/n2hzw6qmz431lc5f40174xsgjr6xy589-home=, System generation 85,
    and Pull generation 5 pass the fresh checks; Home 38 is the immediate
    fallback.
- [X] Accept [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 17: Racket vertical slice][Slice 17: Racket vertical slice]].
  - [X] Assemble the implementation candidate with manifest-only full Racket
    9.1, the explicit runtime-detached Home Racket Mode object, the marked
    source/test/docs fixture, project-keyed PLT wrapper, copied checker, native
    indentation policy, and explicit project backend/XP plus shared key/window
    integration.  The focused package build is 44.0 MiB and its full requisites
    retain no Racket runtime output, avoiding the upstream package's hidden
    2.135 GiB closure.
  - [X] Keep dependency/formatting policy explicit: no =raco pkg=, package
    installation, or catalog mutation; no fake =raco fmt=; native indentation
    only; reject =DROP=/=ADD=/=BYPASS= recommendations from
    =raco check-requires=; run RackUnit, Scribble, and macro expansion through
    the full manifest.  Keep Findutils explicit only in the Racket tier because
    the containment checker inventories files, symlinks, and empty directories.
  - [X] Pass the focused disposable Racket project checker on GuixPC, including
    evaluation/backend at 42, exact keyed PLT state, compilation, lint, tests,
    docs, expansion, direct/Make gates, and source/real-Home mutation checks.
    Its deliberate control emits the expected trigger and exits 91; the unused
    require is diagnosed as =DROP racket/list at 0=.
  - [X] Pass the complete non-activating candidate gate, including detached
    package requisites, Home/System package and executable leakage, batch editor
    tests, the copied project checker, and connected project-keyed backend/XP
    checks.  Syntax, lint, test, documentation, deliberate checker, and connected
    process-failure controls must fail cleanly without source, real-Home,
    user-global package/catalog, source-tree artifact, checker sandbox, or
    process residue.  Project-keyed cache state remains deliberate.  The exact
    gate passes with candidate Home
    =/gnu/store/0qslq27g81snh836mpw4g4n3wgadjq59-home= and unchanged System
    =/gnu/store/6680dpbpy035vs0g3c2f4vkk0sdri4n1-system=.  All 49 batch tests,
    the generic Makefile-owned project policy, authenticated-shell re-entry,
    XP/xref/docs/macro expansion, native debugger recovery, process-group-scoped
    stop, and normal/deliberate connected cleanup checks pass.  The deliberate
    paths return project status 91 and connected outer/inner statuses 1/255.
    Post-gate live links remain accepted Home =n2hzw6qmz431= and System
    =m5znrpyizvkk=, with no checker sandbox or process residue.
  - [X] Publish exact implementation checkpoint
    =18bdc800668070b444c21c5f3f3c0d6b6084e290=.  The historical gate-only
    candidates remain Home =0qslq27g81sn= and System =6680dpbpy035=; neither was
    activated by the gate.
  - [X] Run =ghr= and reboot into Home generation 40 at
    =/gnu/store/by5c2mcdhmxf456q7imb6j56kv2y9s4w-home=.  System generation 85
    remains current and booted at =/gnu/store/m5znrpyizvkkz77nn5nc7swl1jfhp7mf-system=;
    Pull generation 5 remains =/gnu/store/mm52g4iy2hx36vn27h7y13cgc8zqzv5c-profile=.
    The project normal path and deliberate status-91 control, live and 49/49
    batch editor checks, Geiser/SLY connected regression, actual EXWM cold
    =.rkt= open, connected Racket normal markers, and deliberate inner-255/
    outer-1 cleanup all pass.  Persistent runtime/closure leakage, global Racket
    state, checker sandboxes/processes, and repository mutation are absent.
    Retention inventory, fixture, verification, read-only plan, and rollback
    drill pass with Home 40 promoted and Home 39 retained as the immediate
    fallback.
  - [X] Record complete rollback: restore source checkpoint
    =2d6587fe707892acc9dd33f605ccba6ab839c161= first, switch exactly to Home 39
    through the protected Pull 5 Guix, and reboot.  Home rollback alone does not
    restore repository-linked source; System 85 needs no change.
- [X] Accept [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 18: Optional dialect incubation][Slice 18: Fennel incubation]].
  - [X] Select Fennel over Janet, Hy, Chez Scheme, and alternate Common Lisp
    implementations.  It adds a new Lisp-on-Lua family with a complete pinned
    interpreter/formatter/LSP/editor path while keeping only the runtime-free
    editor mode persistent.
  - [X] Implement the manifest-only Fennel 1.6.1, fnlfmt 0.3.0, fennel-ls 0.2.3,
    and Findutils stack; generic authenticated wrapper with canonical immutable
    Guix-store-profile enforcement; SHA-256-keyed isolated state; marked fixture;
    project/editor/connectivity checks; and shared
    REPL/LSP/format/Puni/Org/window integration.  Home adds only
    =emacs-fennel-mode= for 83 declared packages; System remains unchanged.
  - [X] Pass focused project and connected-editor checks plus the complete
    non-activating candidate gate.  Exact gate-only paths are Home
    =/gnu/store/8q8dki5ka0b7ayby2kg1c7gk1cc9mrfh-home= and unchanged System
    =/gnu/store/6680dpbpy035vs0g3c2f4vkk0sdri4n1-system=.  Runtime/tool leakage,
    mutable dependency state, source/repository mutation, and checker-process
    cleanup controls pass.  The exact fennel-ls/Fennel 1.6.1 compatibility
    warning is documented; semantic LSP acceptance is basic non-macro source
    only.
  - [X] Publish implementation checkpoint =c1bbf39= and candidate documentation
    checkpoint =81ebb24= before activation.
  - [X] Run =ghr=, reboot, and pass fresh live project, editor, cold-open,
    two-root REPL/LSP, formatting, basic non-macro LSP, ownership/leakage, and
    deliberate cleanup checks.  Home generation 41 is active at
    =/gnu/store/ci9szqljcz4jjvwcrp76865j8q8ihl3d-home= with profile
    =/gnu/store/bwa6glmgygb8fvw5yj4fvfvk85rqsgdc-profile=; System generation
    85 remains current and booted, and Pull generation 5 is unchanged.
  - [X] Confirm no checker/project residue or repository mutation remains, remove
    both stage-hash cache directories, and rotate exact retention pins.  The
    inventory counts are System/Home/Pull =84/41/5=; Home 40 is the preferred
    immediate Racket fallback and Home 39 the last-known-good Clojure fallback.
    No generation deletion or garbage collection was performed.
  - [X] Record complete rollback: restore source checkpoint
    =81ebb249e299e4e6bd840971d084ab21521d1e27= first, use protected Pull 5 to
    switch exactly to Home 40, and reboot.  System 85 needs no change.
- [X] Complete [[file:../../../docs/audits/2026-07-12-full-configuration-audit.org::*Slice 19: Final desktop, security, and documentation polish][Slice 19: desktop, security, and documentation polish]].
  - [X] Diagnose the post-reboot Org warning as stale native Org 9.8.3 code in
    =~/.emacs.d/eln-cache/30.2-8de1fdd7/= shadowing Home-owned Org 9.8.7.
    The warning reproduces under clean =emacs -Q= with that cache and disappears
    when it is excluded; Fish, Fennel, ELPA, Straight, and config load order are
    not causal.
  - [X] Implement the non-activating correction candidate: tracked early init,
    a profile-store-keyed XDG ELN cache, explicit removal of the legacy fallback,
    preserved Guix native-site paths and JIT, a fifteenth managed repository
    link, and production-parity loading in the generic/Clojure/Racket/Fennel
    isolated editor gates.
  - [X] Pass the causal stale-ELN regression, full isolated editor suite,
    dialect editor gates, Home candidate build, link/reconfigure checks, and
    repository non-mutation checks before publication.  The pinned build gate
    passes with 54/54 ERT checks, Geiser/Guile, SLY/SBCL, Clojure, Racket, and
    Fennel connected backends plus Racket/Fennel deliberate cleanup controls.
    Its exact gate-only paths are Home
    =/gnu/store/rq1q8wnqjk3s3w0q2adiz31g80n5s98i-home= and unchanged System
    =/gnu/store/6680dpbpy035vs0g3c2f4vkk0sdri4n1-system=.
  - [X] Publish implementation checkpoint
    =7476b0527caacb92a6fc1fdd4e05ea4cddd5a794=, activate Home generation 42 at
    =/gnu/store/54fjca4j6z34xmvlvali08awjgyhfxyw-home=, quarantine the complete
    36 MiB/342-file legacy tree at
    =~/.emacs.d/eln-cache.quarantine-20260713-slice19/=, and reboot.  System 85
    and Pull 5 remain unchanged.
  - [X] Accept fresh EXWM Emacs with Home Org 9.8.7, zero mismatch warnings, the
    exact =bwa6glmgygb8fvw5yj4fvfvk85rqsgdc-profile= cache first, and the legacy
    path absent.  Live plus 54/54 isolated editor checks, Geiser/SLY, Clojure,
    Racket, available-hardware desktop/session/audio, entrypoint, editor-tool,
    ownership, and repository non-mutation gates pass; Bose Bluetooth is
    accurately skipped.  Rotate the exact pins to Home 42 as promotion, Home 41
    as preferred pre-correction rollback, and Home 40 as last-known-good Racket
    rollback; inventory, verify, plan, and rollback drill pass without
    generation deletion or garbage collection.
  - [X] Correct the diagnosed Fennel Mode 0.9.2 framing bug from the one-shot
    acceptance failure.  Its buffered splitter calls =setcar= on =nil= when a
    final newline arrives as its own process chunk and can merge a buffered
    partial record with the next record.  Published checkpoint =a8d8222= adds a
    narrow repository-owned compatibility override, deterministic
    edge/split-point ERT coverage, and checker ordering that reports the primary
    editor failure before a missing descendant marker while still running
    residue checks.
    The supported live reload returns =t=; EXWM Emacs has exactly one override,
    and both formerly broken fragments pass.  Batch 56/56, the synthetic
    early-exit fixture, strict shell validation, and one no-retry connected
    normal/deliberate-failure run pass without residue.  L-18 rollback restores
    source checkpoint =f52778c0c8a5a1364f139adfeda396eb235ead54= and reloads
    or restarts Emacs only; it needs no =ghr= or reboot.
  - [X] Record rollback for the native-comp correction: restore source
    checkpoint =e69644a98a32e1c63eb7ea9b5bb313849c7c6d87= first, remove the
    correction-only managed live early-init link if it remains, switch exactly
    to retained Home 41, and reboot.  Keep the known-stale cache quarantined;
    System 85 and Pull 5 do not change.
  - [X] Accept the trusted-physical single-user boundary: retain tty1 autologin,
    keep X cookie access control with no X TCP listener or =xhost= expansion,
    and disable Kitty remote control.
  - [X] Publish =ba6bc65= and =5b39af8= to isolate Picom from ambient
    configuration with =--config /dev/null=, retain Xinit as its sole automatic
    owner, and serialize bounded manual restarts.  The declared Emacs-only
    opacity, 600-second X blanking/DPMS policy, two immediate hot restarts, cold
    session, and final 58/58 editor suite pass.
  - [X] Close Blueman ownership: current Blueman persists only mutable GUI state
    through the private GLib keyfile at
    =~/.config/glib-2.0/settings/keyfile=; no dconf declaration is required.
  - [X] Close audio absence policy: ordinary mode accepts a physical endpoint or
    an accurately classified disconnected/no-endpoint =auto_null= skip; strict
    mode requires the connected Bose as the default BlueZ sink and finite
    playback.
  - [X] Publish =b10bca3=, activate System generation 86 at
    =/gnu/store/qwy0gwwfbaw6x187hh4jh0sk1vr0lry7-system=, and cold-accept
    =AutoEnable = true=.  With the Bose off, the controller starts powered and
    remains non-discoverable/non-pairable; its power button alone triggers
    automatic reconnection and the strict audio gate.
  - [X] Rotate the exact System retention pin to accepted System 86, retain
    System 85 as its immediate fallback, and pass inventory, verification,
    read-only plan, and rollback drill without deletion or garbage collection.
  - [X] Reconcile the README, audit, machine TODO, and historical handoff.  No
    successor stabilization slice is scheduled; custom-kernel work remains a
    separate deferred experiment.

The configuration-audit remediation roadmap is complete through Slice 19.
There is no successor stabilization slice.  Open a new dated audit only for a
newly observed defect or deliberately expanded scope; keep the accepted Fennel
runtime/tool boundary and documented fennel-ls version limitation intact.

Completed stabilization sequence:

#+begin_src text
Slice 9   command, path, formatter, and activation correctness
Slice 10  one session bus and converged desktop services
Slice 11  EXWM startup, reload, and client-placement reliability
Slice 12  reproducible channels, build gates, and retention
Slice 13  package ownership and per-dialect environments
Slice 14  core Lisp editing correctness and structural editing
Slice 15  Elisp, Guile, and Common Lisp project/test workflows
Slice 16  Clojure/JVM vertical slice
Slice 17  Racket vertical slice
Slice 18  optional dialect incubation
Slice 19  final desktop, security, and documentation polish
#+end_src

Do not combine this recovery program with custom-kernel experimentation.  The
kernel tasks below remain deferred behind their own inventory and rollback gates.

* DONE Follow-on Phase 0 and P1.1 SSH terminal contract
CLOSED: [2026-07-15 Wed]

- Evidence: [[file:../../../docs/audits/2026-07-15-phase-0-baseline.org][2026-07-15 Phase 0/P1.1 record]].
- [X] Publish =6c408af= and cold-accept System generation 87 at
  =/gnu/store/gz53dagd09rr97k0ffhkx40rz0am4c88-system= with Kitty's default
  and =terminfo= outputs.
- [X] Preserve truthful =TERM=xterm-kitty=, generic =xterm-256color=, one
  interactive Fastfetch greeting, byte-clean noninteractive SSH, and a real
  Kitty =emacsclient -t= pair that leaves the EXWM server alive.
- [X] Pass desktop/session/audio, strict source, retention, and rollback gates;
  retain System 86 as the immediate fallback and System 85 as an older accepted
  fallback without deleting generations or collecting garbage.
- [X] Record recurrence of the early =init[1]= fault log line, IRQ 7 disable,
  and TSC-to-HPET fallback for later dedicated attribution; do not hide them
  with a terminal, IRQ, or clocksource workaround.  P2.K1a later proved that
  the first line is libgc's handled initrd address-limit probe, not a PID 1
  crash.

* DONE P2.K1a read-only boot-edge attribution
CLOSED: [2026-07-16 Thu]

- Evidence: [[file:../../../docs/audits/2026-07-16-p2.k1a-boot-kernel-attribution.org][2026-07-16 P2.K1a attribution record]].
- [X] Close P0-F01 for the exact =0x3fff00= / =0x4d5593= signature: static
  initrd Guile 3.0.11 was running libgc 8.2.8's intentional handled
  address-limit probe; Shepherd subsequently remained healthy.
- [X] Retain P0-F02 as a recurring AMD GPIO parent-IRQ storm whose source and
  functional impact remain unknown.  The event does not justify =irqpoll= or a
  =pinctrl_amd= blacklist.
- [X] Retain P0-F03 as a recurring TSC/HPET disagreement with a
  defensive automatic HPET fallback and unmeasured performance impact.  The
  faulty side remains unknown, and the event does not justify a clocksource
  override.
- [X] Confirm that accepted retained Systems 75, 80, 81, 83, 84, 85, 86, and
  87 all use Linux 7.0.14, so no retained-kernel control currently exists.
- [X] Exclude P0-F04/P0-F05 and defer them to separate wireless work.
- [X] Make no declaration, boot argument, module, kernel, firmware, generation,
  activation, reboot, or runtime change.  No successor experiment is selected.

* DONE P2.S1a passive system/build/storage baseline
CLOSED: [2026-07-16 Thu]

- Evidence: [[file:../../../docs/audits/2026-07-16-p2.s1a-system-performance-baseline.org][2026-07-16 P2.S1a baseline]].
- [X] Capture CPU policy and idle state, natural-state thermals, memory and
  swap, filesystem and SSD capabilities, Guix daemon/build ownership, current
  store/profile pressure, and process timing on the synchronized System 87
  host.
- [X] Confirm that the point sample showed negligible pressure, cool idle
  sensors, no swap use, about 35 GiB root/store free, about 139 GiB Home free,
  and low inode use; do not promote that to a whole-machine health claim.
- [X] Record that periodic TRIM has no declarative owner and its maintenance
  policy is unselected.  Record SMART data as uncaptured rather than absent:
  inherited UDisks already exposes an ATA SMART interface that was not queried.
  Neither finding is evidence that the SSD is currently failing or slow.
- [X] Keep =--max-jobs=1 --cores=4=, HPET cost, sustained thermals, and true
  boot-to-ready time as measurement hypotheses or unknowns, not tuning
  findings.
- [X] Preserve the 44 unprotected older same-target generation-link candidates
  as Phase 5 inventory only.  At P2.S1a closure, backup/restore evidence remained
  a hard gate before generation cleanup or store collection; P4.2 later
  superseded that unconditional gate only for separately reviewed Guix
  generation/store maintenance.
- [X] Make no declaration, package, mount, service, governor, scheduler, swap,
  build-policy, generation, garbage-collection, activation, runtime, or reboot
  change.  P2.S1b/P2.S1c remain unpromoted.

* DONE P2.M1 storage-maintenance contract design
CLOSED: [2026-07-16 Thu]

- Evidence: [[file:../../../docs/audits/2026-07-16-p2.m1-storage-maintenance-contract.org][2026-07-16 P2.M1 contract]].
- [X] Correct P2.S1a: periodic TRIM ownership is absent but policy was
  unselected; inherited UDisks already exposes an ATA SMART path.
- [X] Disclose the exploratory UDisks D-Bus lazy activation and initial
  read-only SMART metadata housekeeping.  Retain no raw serial/WWN output and
  claim no setting, self-test, generation, or declaration change.
- [X] Record allowlisted cached SMART properties and selected attributes with
  strict interpretation limits.
- [X] Define a no-new-package Scheme reader over =busctl --json=short= and
  Guile =(json)=, including cached-only mode, fixture gates, forbidden methods,
  and identifier rejection from retained/rendered output.  Acknowledge the
  unavoidable transient Drive-path argv boundary of the =busctl= transport.
- [X] Map Guix/Shepherd periodic-TRIM command, root privilege, schedule,
  missed-event, logging, dry-run, activation, and one-way FITRIM boundaries.
- [X] Keep visibility, TRIM policy/dry-run, declarative timer activation, actual
  FITRIM, deep diagnostics/alerting, and backup-gated cleanup separate.

* DONE P2.M1a Scheme UDisks SMART reader
CLOSED: [2026-07-16 Thu]

- Evidence: [[file:../../../docs/audits/2026-07-16-p2.m1a-scheme-disk-health-source-checkpoint.org][2026-07-16 P2.M1a source checkpoint]].
- Runtime evidence: [[file:../../../docs/audits/2026-07-16-p2.m1a-cached-only-runtime-acceptance.org][2026-07-16 P2.M1a cached-only acceptance]].
- Source implementation and offline fixtures pass.  The pushed checkpoint and
  one cached-only live read also pass; Rafael explicitly accepted completion
  at that selected boundary.
- [X] Implement the fixed Scheme reader and launcher with human, JSON, summary,
  cached-only, and allow-activate presentation/policy modes.
- [X] Pass the 185-check fake-runner protocol, classification, lifecycle,
  failure, privacy, and exact-command fixture gate without system-bus access.
- [X] Pass warning-clean compilation, focused ShellCheck, forbidden-operation,
  hard-coded-private-path, and privacy source gates.
- [X] Commit the source checkpoint.
- [X] Push the checkpoint and verify upstream equality.
- [X] Authorize and run one cached-only live read; document only sanitized
  output and confirm that it did not request UDisks D-Bus auto-start.
- [X] Ordinary UDisks D-Bus activation remains unselected and was not required
  by the accepted P2.M1a scope.
- [X] Accept P2.M1a completion.
- Add no package, root-service declaration, sudo rule, setuid helper,
  System/Home generation, device setting, SMART refresh/self-test, alert,
  trim, or reboot merely to close this slice.

* DONE P3.1 visual ownership baseline and semantic-token contract
CLOSED: [2026-07-16 Thu]
:PROPERTIES:
:created: 2026-07-16
:completed: 2026-07-16
:END:

- Evidence: [[file:../../../docs/audits/2026-07-16-p3.1-visual-ownership-and-token-contract.org][2026-07-16 P3.1 visual ownership and token contract]].
- [X] Verify the synchronized GuixPC checkout, active System/Home, live
  entrypoints, and current visual owners without changing runtime state.
- [X] Map Emacs, Kitty, Fish, GTK 3/4, fonts, icons, cursor, Picom, wallpaper,
  and X-resource ownership with explicit observed/default/fallback boundaries.
- [X] Record the live Emacs / declared Kitty canvas match and target-specific
  opacity values without claiming live Kitty parser evidence or a unified
  palette.
- [X] Define the contract for a data-only Scheme schema, pure
  validator/renderers, deterministic target set, drift checks, migration
  stages, rollback, and stop conditions; do not imply those files exist yet.
- [X] Correct the earlier architecture: repository policy forbids Kitty
  includes, so later work must render one complete immutable =kitty.conf=.
- [X] Review and accept the contract with Rafael.
- [X] Push checkpoint =8933827= and verify clean upstream equality at =0/0=.
- At P3.1 closure, keep fixture-only P3.2 implementation, P3.3 Home
  wiring/build, and P3.4 activation/live visual acceptance separate.
  Production tokens/outputs were blocked until the palette authority, GTK
  base, icon, cursor, scaling, contrast, Fish, test surface, and UI size were
  accepted; the P3.2 addendum records their later acceptance.
- Add no visual source value, package, build, generation, activation, session
  reload/restart, GUI launch, or reboot merely to close P3.1.

* DONE P3.2 offline semantic-token schema, renderers, fixtures, and production datum
CLOSED: [2026-07-17 Fri]
:PROPERTIES:
:created: 2026-07-16
:completed: 2026-07-17
:END:

- Evidence: [[file:../../../docs/audits/2026-07-16-p3.2-offline-theme-schema.org][2026-07-16 P3.2 offline semantic-theme schema, fixture, and production gate]].
- Accepted production inputs: frozen Emacs 30.2 =modus-vivendi-tinted= subset;
  JetBrainsMono Nerd Font families; GTK 3 =Adwaita-dark= / GTK 4 =Adwaita=;
  =Papirus-Dark=; =Bibata-Modern-Ice= at 32 px; logical 96 DPI and scale 1;
  11 pt UI text (including Emacs variable-pitch, while default/fixed remains
  12 pt);
  7:1 / 4.5:1 / 3:1 contrast floors; first-migration Fish ownership;
  =gtk4-widget-factory=; and the current wallpaper/fit policy.
- This records the historical P3.2 decision.  P3.4's later real-display A/B
  invalidated only the GTK 3 alias: this Guix GTK 3 reports the configured
  =Adwaita-dark= string but renders the light fallback.  Its corrected
  production successor is =Adwaita= plus
  =gtk-application-prefer-dark-theme=true=.
- [X] Implement the pure =(sk theme)= schema/validator and all six renderers
  without Home wiring, activation, GUI launch, or runtime mutation.
- [X] Add unmistakably synthetic complete fixtures, deterministic goldens,
  negative controls, explicit-root asset checks, and aggregate source-gate
  integration.
- [X] Pass 191 Scheme checks and pinned native Fish 4.7.1, Emacs 30.2, and
  Kitty 0.47.4 source parsers; pass the complete strict repository source gate,
  direct ShellCheck/diff checks, and changed-file Org lint.
- [X] Accept GTK/UI text at 11 pt and logical 96 DPI.  Use 11 pt for Emacs
  variable-pitch while preserving Emacs default/fixed at 12 pt and Kitty at
  14 pt.
- [X] Publish fixture-only source checkpoint =3627f0b= and verify clean
  upstream equality at ahead/behind =0/0= before changing the production gate.
- [X] Add schema version 2 and inert production =theme/tokens.scm= with exact
  Modus Themes 4.4.0 provenance, source hashes, role/ANSI identity freeze, and
  explicit project-derived aliases.  Exempt only exact terminal =black= and
  =bright-black= dark/dim endpoints from the 3:1 ANSI-on-canvas floor; keep the
  other 14 ANSI slots and semantic pairs enforced.
- [X] Pass the extended production/golden/native-parser source gate: 281
  Scheme checks, pinned native parsers, complete strict repository gate,
  changed-file/agenda Org lint, and clean diff check.
- [X] Publish production source checkpoint
  =b74a98d736b6dfbca421b115d1a0c979d707a853= and verify clean upstream
  equality at ahead/behind =0/0=.
- [X] Preserve separate source, candidate Home evaluation/build, checkpoint,
  Home activation, live application, real-display, and rollback gates for
  P3.3/P3.4.  P3.2 performs no Home build or runtime mutation.

* DONE P3.3 immutable Guix Home theme wiring and candidate build
CLOSED: [2026-07-17 Fri]
:PROPERTIES:
:created: 2026-07-17
:completed: 2026-07-17
:END:

- Evidence: [[file:../../../docs/audits/2026-07-17-p3.3-immutable-theme-home-wiring.org][2026-07-17 P3.3 immutable Home wiring and candidate build]].
- [X] Build one isolated Scheme-generated bundle and map its six adapters,
  token datum, and wallpaper through Guix Home.
- [X] Add exact font/icon/cursor package ownership and =gtk:bin= for
  =gtk4-widget-factory= without broadening Qt or scaling scope.
- [X] Move Kitty from the live repository-link manifest to a guarded retired
  transition in the same candidate that gives Guix Home its complete immutable
  config; retain =kitty/kitty.conf= for generation-42 rollback.
- [X] Build and validate candidate
  =/gnu/store/b668045vxgnx7mnsi99pf4nsdd4md9hc-home= while Home generation 42
  and every prior live owner remain unchanged.
- [X] Pass the integrated source, 60-test Emacs/backend, immutable-candidate,
  and strict repository gates.
- [X] Pass the complete pinned build gate, including development manifests,
  Home/System builds, realized ownership, non-login SSH Fish, and connected
  editor backends.
- [X] Commit implementation checkpoint
  =530eb250e2e876f20b3c553be39533e5548cfb6d= after the complete source/build
  gates pass.
- [X] Publish implementation =530eb25= and evidence =00bf260=, fetch them,
  and prove clean =HEAD=/=origin/main= equality at ahead/behind =0/0= before
  activation.
- [X] Perform no Home activation, consumer restart, GUI launch, EXWM/session
  restart, or reboot in P3.3.
- [X] Preserve the historical P3.3 result and cross-reference the later P3.4
  finding that =gtk:bin= alone did not expose GTK's private schemas.

* DONE P3.4 activation and real-display visual acceptance
CLOSED: [2026-07-18 Sat]
:PROPERTIES:
:created: 2026-07-17
:completed: 2026-07-18
:END:

- Evidence: [[file:../../../docs/audits/2026-07-17-p3.4-theme-activation.org][2026-07-17 P3.4 GTK/GStreamer activation stops and corrections]].
- [X] Capture the exact P3.3 source checkpoint, clean upstream equality,
  generation 42/result, retained Kitty source, and rollback/retention plan.
- [X] Run the read-only 14-live-plus-one-retired link preflight, then run =ghr=
  and record Home generation 43 at =1slsnsfv...-home= plus the exact legacy
  Kitty backup.  Treat this as activation evidence only.
- [X] Before restarting consumers, prove all generated targets are store-backed
  and golden-equal, Kitty has one Home owner, and all 14 continuing repository
  links remain correct.
- [X] Test fresh Kitty, Fish, Emacs, and Blueman/GTK 3 separately; all remain
  alive or pass their exact non-GUI assertions.
- [X] Stop before session restart when =gtk4-widget-factory= aborts on missing
  =org.gtk.gtk4.Settings.ColorChooser=; make no visual-acceptance claim.
- [X] Trace the false positive: =gtk:bin= references =gtk:out= in its closure
  but does not expose its schemas through the Home profile.
- [X] Correct ownership to exact =gtk:out= plus =gtk:bin= while retaining 89
  unique package names and recording 90 actual package/output selections.
- [X] Strengthen output/provenance/schema/cache gates and independent negative
  controls; pass the complete source gate, pinned Home dry-run, a Home build
  whose profile has 94 top-level records, and the immutable candidate check at
  =yr959z61...-home=.
- [X] Publish GTK correction =1358cb2=, fetch it, and prove clean
  =HEAD=/=origin/main= equality at ahead/behind =0/0=.
- [X] Re-run the preflight and activate Home generation 44 at
  =gb9kvfwn...-home=.  Prove its 94-record profile exposes both GTK outputs,
  the ColorChooser XML/cache, and exact declared provenance.
- [X] Stop again when the generation-44 exact-profile factory removes the
  schema error but aborts with status 133 on missing GStreamer =playbin3=.
  Perform no session restart, reboot, or visible-acceptance claim.
- [X] Trace the second false positive: GTK's closure references GStreamer, but
  generation 44 has no base/good records, plugin links, or Home
  =GST_PLUGIN_SYSTEM_PATH=.  Treat the running EXWM environment as stale until
  the session is restarted.
- [X] Directly own =gst-plugins-base= and =gst-plugins-good=.  Retain 89
  ordinary specifications, two exact GTK output selections, one explicit
  package, 92 total selections, and 91 unique names.
- [X] Strengthen plugin provenance, exact generated search-path, versioned and
  unversioned hostile-environment isolation, fresh-registry element, and
  negative-control gates.  Pass the complete source gate and the 96-record
  immutable candidate at =99xy9a0...-home=.
- [X] Publish GStreamer correction =d83feb8=, fetch it, and prove clean
  =HEAD=/=origin/main= equality at ahead/behind =0/0=.
- [X] Re-run the preflight and activate generation 45 at =9rmcgpl4...-home=.
  Prove its active 96-record profile exposes both GTK outputs, both GStreamer
  owners, exact plugin links, the generated plugin search path, and all six
  required GStreamer elements with an isolated registry.  Preserve EXWM Emacs
  PID 6631, Picom PID 6647, and Kitty PID 9010 through activation.
- [X] From a clean =env -i= shell that sources =~/.profile=, launch the exact
  active-profile
  =gtk4-widget-factory= with an isolated registry and without manual schema,
  data, or plugin-directory overrides.  It remains alive for five seconds with
  no fatal GLib or GStreamer diagnostic.  The only log output is the known
  nonfatal 896-versus-1445 EXWM allocation warning.
- [X] Reboot into fresh EXWM twice.  Prove the new Emacs inherited the active
  Home =GST_PLUGIN_SYSTEM_PATH=; repeat the isolated factory launch through the
  real Emacs environment; obtain Rafael's positive visible GTK 4 result; and
  terminate every diagnostic client.
- [X] Prove there is no structural keymap/grab regression:
  all 30 reviewed EXWM global bindings and commands are installed, the private
  grabbed-key set matches, and the startup log has no =BadAccess= or grab
  failure.
- [X] Stop broader visual acceptance on two reproducible findings.  GTK 3
  reports =Adwaita-dark= but renders its widget factory and Blueman light.
  Emacs creates its first X frame before loading the generated theme and
  applies bar/fringe policy in normal init; EXWM then creates four additional
  frames before its deferred fullscreen transition.
- [X] Prove the GTK 3 correction process-locally: with no session-wide
  =GTK_THEME=, =Adwaita= plus the existing prefer-dark setting renders both
  exact-profile GTK 3 widget factory and Blueman dark.  Reject XSettings,
  GSettings/dconf, and application-specific overrides.
- [X] Build the source correction in an isolated checkout: move immutable
  theme consumption and all eight creation-time frame parameters into
  =early-init.el=; make generated symbol fallbacks idempotent per graphical
  frame; and freeze GTK 3 =Adwaita= with prefer-dark.  Pass 298 theme checks
  plus native parsers, 61 Emacs tests plus connected backends, the immutable
  Home fixture, and the strict repository gate.
- [X] Build and validate the realized Home candidate
  =/gnu/store/dkasc56ngxmyg4gq453xx71h5xzaka3m-home=.  Its unchanged
  96-record profile, corrected GTK 3 adapter, generated per-frame Emacs helper,
  complete immutable checker, five-frame fontset behavior test, and pinned
  Home/System gate pass.
- [X] Publish correction =e6492cc=, fetch it into the actual clean checkout,
  prove upstream equality, run guarded Home activation as generation 46 at
  =mf9jzh...-home=, and retain generation 45 as rollback.
- [X] Reboot the activated correction.  Confirm all five EXWM frames are
  fullscreen, undecorated, bar-free, and generated-theme/font configured;
  confirm the normal GTK 3 widget factory and Blueman are dark; confirm
  keybindings behave normally; and terminate both probes.
- [X] Attend boot =4832d067...=.  Confirm the original titlebar, scrollbar,
  decoration, theme, and keybinding findings do not recur; stop no-flash
  acceptance on the remaining dashboard small-frame-to-fullscreen takeover.
- [X] Attribute that takeover to EXWM 0.35's real post-trace initialization,
  1x1 mapped containers, and deferred fullscreen work.  Reject hidden frames:
  four inactive workspaces remained 656x612 in the isolated X test.
- [X] Build the mapped-opacity source candidate.  Start Picom synchronously,
  keep all five startup frames mapped at zero opacity, yield to EXWM's deferred
  fullscreen timers, poll selected/active geometry without blocking them,
  pre-render while transparent, then reveal at 85 percent and remove the
  one-shot owners.  Pass 299 theme checks, 78 Emacs tests, the fresh-process
  watchdog-registration failure fixture, connected backends, the strict source
  gate, a controlled nested-X pixel proof, and four no-pause nested-X
  lifecycle trials.
- [X] Publish implementation =f24b7b5= and evidence =5c579da=, fetch them into
  the clean actual checkout at upstream equality =0/0=, activate generation 47
  at =/gnu/store/84pm5bd8kpm6gylx0378dcgdsazh6a49-home=, and attend boot
  =6c560dab...=.  The exact Picom, five-frame geometry/opacity, gate-cleanup,
  desktop-session, 78-test Emacs, and connected-backend checks pass.  Rafael
  reported that startup was “SO much better!”; record that as strong positive
  evidence without silently upgrading it to an absolute zero-transition
  claim.
- [X] Accept wallpaper/ultrawide fit, Picom transparency, cursor/icons,
  font/contrast, GTK 4, and Fish syntax/pager colors on boot =b36bfc06...=.
- [X] Complete adjacent keybinding, SSH, terminal, and EXWM acceptance with
  Rafael.  The final desktop-session run passes with physical-audio and
  Bluetooth endpoint checks explicitly skipped because they were disconnected.
- [X] Preserve two-sided Kitty rollback evidence and the generation-46
  immediate rollback.  Do not claim Qt, forced DPI, global toolkit-scale
  ownership, connected audio/Bluetooth retesting, or absolute
  zero-transition behavior.

* DONE P4.1 recovery inventory and backup decision packet
CLOSED: [2026-07-18 Sat]
:PROPERTIES:
:created: 2026-07-18
:completed: 2026-07-18
:END:

- Evidence: [[file:../../../docs/audits/2026-07-18-p4.1-recovery-inventory.org][2026-07-18 P4.1 recovery inventory and backup decision packet]].
- [X] Verify clean upstream equality, boot/system/home/pull identity, storage
  topology, mounted destinations, backup clients/services, and generation
  retention without reading user or secret contents.
- [X] Inventory Org, repositories, SSH/GPG, Chromium, Downloads, selected
  editor state, same-disk migration backups, caches, and declarative
  entrypoints by metadata only.
- [X] Confirm restrictive SSH/GPG ownership and modes, record public
  fingerprints and key expiry, and separate permission correctness from
  off-machine recovery coverage.
- [X] Record that both project repositories were clean and synchronized to
  GitHub at the pre-source inventory baseline while Org and private/mutable
  state remained uncovered.
- [X] Correct =generation-pins.tsv= to accepted Home 47/46
  promotion/preferred roles while retaining Home 42/41 as older
  =last-known-good= history.  Perform no generation switch or deletion.
- [X] Record the unexpected =guix gc --list-dead= side effect: 26
  already-dangling automatic roots were pruned; no store object or generation
  was deleted; after-state retention verification passes.
- [X] Define the recovery dependency order and the exact data, destination,
  encryption, RPO/RTO, cadence, retention, and notification choices required
  before P4.2.
- [X] At the P4.1 closure boundary, keep Phase 5 deletion, collection,
  quarantine, and Git cleanup blocked until a representative off-machine
  restore succeeds.  The later P4.2 decision supersedes that unconditional
  gate only for separately authorized Guix generation/store maintenance.
- [X] Add no backup package/service, write no backup payload or secret, change
  no permission, activate no configuration, restart no process, and perform no
  reboot.  Temporary source/roadmap staging is not backup evidence.

* DONE P4.2 recovery/storage deferral and maintenance-policy decision
CLOSED: [2026-07-18 Sat]
:PROPERTIES:
:created: 2026-07-18
:completed: 2026-07-18
:END:

- Evidence: [[file:../../../docs/audits/2026-07-18-p4.2-recovery-storage-deferral.org][2026-07-18 P4.2 recovery/storage deferral and maintenance-policy decision]].
- [X] Accept no backup product, destination, schedule, retention, restore
  drill, storage migration, or hardware purchase for now.  Preserve the
  P4.1-recorded exposure of local-only mutable state as explicit residual risk.
- [X] Perform no mutation of the added Samsung 860 EVO 250 GB SSD, serial
  =S59WNZFN905509A=, through P4.2 automation and leave it unmounted and
  undeclared.  Record Rafael's deliberate removal of its prior
  Windows-oriented FAT/Btrfs layout and the final observation of one
  filesystem-signature-free Linux partition.  No further repartitioning or
  storage role is selected or authorized.
- [X] Record the current-layout, Home-migration/root-expansion,
  separate-store, local-backup, and future-M.2 alternatives.  Prefer measured
  generation/store maintenance first; park any partition migration behind a
  separate offline and irreversible gate.
- [X] Backlog a private remote Org repository as synchronization/history only.
  Current Org remains local-only until provider, privacy, exclusions,
  attachment, authentication, conflict, and clone/restore behavior are
  reviewed.
- [X] Adopt the inert future recovery floor as current, booted System when
  different, five newest additional distinct closures, and all explicit pins.
  Duplicate links do not consume distinct-closure slots.
- [X] Keep a no-mutation estimator, generation-link deletion, and
  unreachable-store collection as separate P5.1/P5.2/P5.3 gates.  Each
  mutation requires exact candidates and explicit attended authorization;
  =guix gc --list-dead= remains classified as mutating.
- [X] Preserve separate recovery requirements for user-data deletion, secret
  handling, Home quarantine deletion, and storage migration.  The owner risk
  decision does not authorize those operations.
- [X] Make no disk, mount, filesystem, package, service, backup, restore,
  generation, GC, activation, process, or reboot change.

* DONE P5.1 read-only retention and closure-accounting estimator
CLOSED: [2026-07-18 Sat]
:PROPERTIES:
:created: 2026-07-18
:completed: 2026-07-18
:published: 93619e90bfb01a43a1c4858be9a09ef76f818eb6
:END:

- Evidence: [[file:../../../docs/audits/2026-07-18-p5.1-read-only-retention-estimator.org][2026-07-18 P5.1 read-only retention and closure-accounting estimator]].
- Exact data: [[file:../../../docs/audits/data/2026-07-18-p5.1-retention-estimate.tsv][329-row live estimator snapshot]].
- [X] Preserve =plan= as the duplicate-only reporter and add a distinct
  no-mutation =estimate= action implementing current, booted System when
  different, five newest additional distinct closures, and every explicit pin.
- [X] Classify exact retained/candidate closures and generation links.  The
  live snapshot retains System/Home/Pull =8/17/5= closures and links and
  classifies =78/30/0= candidate links across =51/13/0= unretained closures.
- [X] Use only reviewed read-only Guix store APIs; reject collector, root
  registration, substitution, build, realization, deletion, and hidden apply
  paths in the fixture.
- [X] Report NAR forward-reference footprints with explicit derivation-policy,
  process-root, temporary-root, and filesystem-allocation gaps.  Guaranteed
  reclaimable bytes remain exactly zero.
- [X] Pass ShellCheck, shell syntax, the expanded isolated fixture, two
  byte-identical live runs, pre/post retention verification and rollback drill,
  and a 32,831-record before/after no-mutation snapshot.
- [X] Published checkpoint =93619e9= is fetched into the actual checkout with
  =HEAD= equal to =origin/main=, a clean tree, and ahead/behind =0/0=.
- [X] At P5.1 closure, kept P5.2 generation deletion and P5.3 store collection
  in backlog.  Neither exact estimator output nor publication authorized
  mutation.
- [X] Change no generation link, store object, profile, pin, package,
  configuration, activation, backup, disk, boot state, or persistent process
  state.

* DONE P5.2a reviewed Home generation deletion
CLOSED: [2026-07-18 Sat]
:PROPERTIES:
:created: 2026-07-18
:completed: 2026-07-18
:plan_published: 450f396068d0c6e9c83186ae5599a06524fe694c
:END:

- Evidence: [[file:../../../docs/audits/2026-07-18-p5.2-reviewed-generation-deletion.org][2026-07-18 P5.2 reviewed generation-deletion preflight]].
- Postflight: [[file:../../../docs/audits/2026-07-18-p5.2a-home-generation-deletion.org][2026-07-18 P5.2a Home generation-deletion canary]].
- Fresh estimate: [[file:../../../docs/audits/data/2026-07-18-p5.2-preflight-estimate.tsv][329-row P5.2 estimator snapshot]].
- Review manifest: [[file:../../../docs/audits/data/2026-07-18-p5.2-generation-deletion-review.tsv][exact Home-canary review manifest]].
- Postflight estimate: [[file:../../../docs/audits/data/2026-07-18-p5.2a-home-deletion-postflight-estimate.tsv][286-row P5.2a estimator snapshot]].
- [X] Reproduce the complete P5.1 estimate byte-for-byte at SHA256
  =1fe7ab18d7fb4e7d3c6ed0e3fd148f796d36d1122a80f1aa903165dd7eee8e11=
  and pass the 32,831-record no-mutation gate.
- [X] Select only Home generations =1..28,30,31= for the first manual
  cycle.  Retain =29,32..47= and expect Home links =47 -> 17=.
- [X] Record every retained, selected, and held raw/canonical link tuple in
  review manifest
  =75c8e4a40d7cacb32ab8324d50001b09d2450972a57eb142112cf8ae07edbb2d=.
- [X] Hold all 78 System candidates.  The supported System command also
  rebuilds GRUB from a default bootloader configuration that omits GuixPC's
  declared EFI target, ultrawide theme/gfxmode, and keyboard layout.
- [X] Publish and fetch review checkpoint =450f396= with clean upstream
  equality.
- [X] Obtain explicit external authorization naming P5.2a and the immutable
  review-manifest hash, thereby binding its exact selector, expected
  =47 -> 17= transition, and System/Pull/GRUB/store/GC prohibitions.
- [X] Run the one reviewed Home command, prove exact link/pointer/pin/store/
  GRUB state plus =verify= and =rollback-drill=, and stop.
- [X] During preflight, change no generation, store object, profile, pin,
  package, configuration, activation, disk, bootloader, backup, boot state, or
  existing persistent process/session/service state.

* BACKLOG P5.2b System generation deletion and custom-GRUB boundary
:PROPERTIES:
:created: 2026-07-18
:blocked_by: D4a/D4b fused-program gates; D4c.1/D4c.2 live manifest/program publication; D4c.3 packet acceptance; separate D5 execution grant
:END:

- Evidence: [[file:../../../docs/audits/2026-07-18-p5.2b-custom-grub-safe-system-pruning-design.org][2026-07-18 P5.2b custom-GRUB-safe System-pruning design]].
- Production-boundary design: [[file:../../../docs/audits/2026-07-19-p5.2b-d4-production-boundary-design.org][2026-07-19 P5.2b-D4 production-boundary safety decomposition]].
- Hold all 78 System candidates.
- Do not run the installed supported System deletion command: after removing
  links it reconstructs GRUB from default bootloader configuration rather than
  GuixPC's declared EFI target, ultrawide theme/gfxmode, and keyboard layout.
- Do not perform exact-link-only deletion: the 78 candidates account for 156
  current GRUB path occurrences, and the current =bootcfg= does not root every
  old System closure.
- P5.2a and P5.2b-D1 do not authorize this live stage.

** DONE P5.2b-D1 source and synthetic-fixture pruning primitive
CLOSED: [2026-07-18 Sat]
:PROPERTIES:
:created: 2026-07-18
:END:

- [X] Review pinned Guix deletion/reinstall order and record the lost custom
  bootloader fields, sequential-link failure boundary, and rejected generic
  package-profile path.
- [X] Add a SHA-bound =FIXTURE-ONLY= exact-tuple verifier and rollback
  primitive that requires a sentinel, refuses root =/=, exposes no live
  =apply=, and carries =authorization state NOT-GRANTED=.
- [X] Pass staged apply/rollback, partial recovery, occupied-path, drift,
  injected-failure, and signal-interruption fixtures without changing
  synthetic Home/Pull, GRUB, =bootcfg=, pins, or store names.
- [X] Correct =scripts/guix-generation-retention plan= so it keeps System
  candidates visible but emits no System deletion command.
- [X] Keep all live System generation links, GRUB, =bootcfg=, store objects,
  profiles, EFI state, and boot state unchanged.

** DONE P5.2b-D2a retained-only bootcfg source and fixtures
CLOSED: [2026-07-18 Sat]
:PROPERTIES:
:created: 2026-07-18
:END:

- Evidence: [[file:../../../docs/audits/2026-07-18-p5.2b-d2a-retained-only-bootcfg-source.org][2026-07-18 P5.2b-D2a retained-only bootcfg source checkpoint]].
- [X] Load GuixPC's real bootloader declaration, require its exact
  EFI/ultrawide/keymap contract, and reject prospective-System construction.
- [X] Use accepted generation 87's canonical System for the direct-store main
  entry and exactly old links =86,85,84,83,81,80,75=.
- [X] Add the closed =STAGE-ONLY= / =NOT-GRANTED= / =REVIEW-ONLY= input
  specification, configuration and semantic validators, and exact accepted-
  GRUB byte projector.
- [X] Bind the exact implementation/evaluation bytes, Pull-profile store item,
  canonical specification, sanitized Guile search path, and D2a-introduction
  Git checkpoint.
- [X] Pass positive/negative native Scheme fixtures, warning-free
  =guild compile -W3=, ShellCheck, and the read-only 5,163-byte live
  projection.
- [X] Expose only =fixture-check= and unprivileged =stage=; add no bootloader
  installation, link deletion, GC, root promotion, reconfiguration,
  activation, or reboot action.

** DONE P5.2b-D2b uninstalled retained-only bootcfg artifact
CLOSED: [2026-07-19 Sun]
:PROPERTIES:
:created: 2026-07-18
:END:

- Evidence: [[file:../../../docs/audits/2026-07-19-p5.2b-d2b-uninstalled-retained-only-bootcfg.org][2026-07-19 P5.2b-D2b uninstalled retained-only bootcfg artifact]].
- [X] From clean =HEAD == origin/main=, run only the unprivileged immutable
  store build and require byte equality with the accepted-GRUB projection.
- [X] Freeze the exact generated =grub.cfg= bytes, SHA256, size, source
  configuration tuple, semantic result, and reviewed pre/post surfaces in the
  repository.
- [X] Report immutable store realization explicitly; do not claim whole-store
  or whole-machine byte identity and do not make the result active.

** DONE P5.2b-D3a recovery transaction source and fixtures
CLOSED: [2026-07-19 Sun]
:PROPERTIES:
:created: 2026-07-18
:published: 676b430617b6902c8d7111bd78a483282de314ae
:END:

- Evidence: [[file:../../../docs/audits/2026-07-19-p5.2b-d3a-recovery-transaction-source.org][2026-07-19 P5.2b-D3a recovery transaction source checkpoint]].
- [X] Finish the fixture-only Scheme engine and closed synthetic manifest for
  per-generation direct recovery roots, exact GRUB/=bootcfg=/link phases, and
  pre-commit rollback versus post-commit forward cleanup.
- [X] Validate the hash-chained, =fdatasync= plus atomic-rename plus
  directory-=fsync= journal and both non-blocking cooperative locks without
  claiming exclusion of arbitrary =guix system= commands.
- [X] Execute and review all 98 declared interruption points: 54 forward and
  44 rollback.  These are post-operation process-restart boundaries, not
  arbitrary-instruction or power-loss recovery.  The exact standalone matrix
  and strict repository gate pass.  D3b's later clean-published compile found
  two generated unused-variable diagnostics; D3a.1 owns their correction.
- [X] Create no live recovery root, transaction directory, profile lock,
  GRUB/=bootcfg= temporary, generation-link mutation, or authorization.

** DONE P5.2b-D3a.1 compile-gate correction
CLOSED: [2026-07-19 Sun]
:PROPERTIES:
:created: 2026-07-19
:base_published: 676b430617b6902c8d7111bd78a483282de314ae
:published: 0601b9d8537afb36b9e3589c6227e8cf652336e1
:END:

- Evidence: [[file:../../../docs/audits/2026-07-19-p5.2b-d3a-compile-gate-correction.org][2026-07-19 P5.2b-D3a compile-gate correction]].
- [X] Reproduce the two clean-published Guile 3.0.11 =guild compile -W3=
  unused =failure= diagnostics before changing source.
- [X] Prevalidate the two closed row shapes and remove only their unreachable
  final =ice-9 match= catch-all clauses; rebind the exact module, manifest, and
  harness hashes.
- [X] Require zero compile diagnostics, all =54 + 44 = 98= recovery points,
  the full strict repository gate, and an exact =121=-record live-snapshot
  comparison.
- [X] Keep D3b open until this correction is published and fetched from a
  clean upstream-equal checkout.

** DONE P5.2b-D3b clean-published recovery evidence
CLOSED: [2026-07-19 Sun]
:PROPERTIES:
:created: 2026-07-19
:source_published: 0601b9d8537afb36b9e3589c6227e8cf652336e1
:evidence_publication: b1d6ecb38c495089ab22e4e648a1fce9cd48bc1b
:END:

- Evidence: [[file:../../../docs/audits/2026-07-19-p5.2b-d3b-clean-published-recovery-evidence.org][2026-07-19 P5.2b-D3b clean-published recovery evidence]].
- [X] Fetch the exact published corrected D3a.1 checkpoint into a clean
  =HEAD == origin/main= checkout and bind the final implementation, manifest,
  registry, and fixture hashes.
- [X] Rerun the complete 98-point native matrix, compilation, ShellCheck,
  strict repository gate, whitespace check, and Org lint; freeze exact output
  and non-mutation evidence.
- [X] Track the closed 54-record result, four raw channels, and byte-identical
  129-record selected-surface snapshot without claiming whole-store or
  whole-machine identity.
- [X] Stop without exercising any live transaction surface or granting D4
  authorization.

** DONE P5.2b-D4a reconciliation and fused-program source/fixtures
CLOSED: [2026-07-19 Sun]
:PROPERTIES:
:created: 2026-07-19
:blocked_by: none; source/fixture work only
:published: 41e11155f817c8ccf2f8e8b3c9c62af566f53209
:END:

- Design: [[file:../../../docs/audits/2026-07-19-p5.2b-d4-production-boundary-design.org][2026-07-19 P5.2b-D4 production-boundary safety decomposition]].
- Evidence: [[file:../../../docs/audits/2026-07-19-p5.2b-d4a-reconciliation-fused-source.org][2026-07-19 P5.2b-D4a reconciliation and fused-program source checkpoint]].
- [X] Implement the closed, fail-closed reconciliation table for exact
  pre-=BEGIN= directory states, absent/complete/provable-prefix partial backup,
  the complete direct-program-root-first root/transaction-base/lock/
  candidate-root/transaction-directory/quarantine/journal bootstrap prefix,
  and unique known journal/GRUB/=bootcfg= temporaries.  Unknown or ambiguous
  state must remain =REVIEW-REQUIRED= without cleanup.  Production recovery
  resumes the exact prefix and never reverse-unlinks persistent lock files or
  the program root before a durable terminal event.
- [X] Reject every hash-valid but illegal journal history with one closed
  forward/rollback/post-commit automaton, including duplicates, skipped or
  reordered transitions, and any suffix after a terminal event.
- [X] Implement the deterministic constructor/render source for one fused
  Scheme file containing the exact transaction implementation and fixture
  manifest bytes.  Validate emitted bytes without realizing a store output;
  D4b owns the first =computed-file= realization and store-location tests.
- [X] Retain the cooperative transaction and System-profile locks, require an
  attended quiescence observation, fixture-test transaction-then-System
  no-wait acquisition after the durable program root, and run one exact
  protected-surface/root/journal revalidation gate under both locks before
  every side-effecting forward and rollback phase.  Do not claim exclusion of
  arbitrary =guix system= commands.  Retain both exact lock inodes as terminal
  infrastructure; D5 never unlinks a persistent lock pathname.
- [X] Add the one-store-connection root abstraction and synthetic tests for
  temporary roots plus direct manifest-keyed roots for the fused program,
  every selected target, and old/new =bootcfg=.  Cover missing/retargeted/
  invalid/non-live roots, liveness loss, session failure, partial bootstrap,
  and cleanup.  D4a proves no production registration or liveness.
- [X] Keep D4a =FIXTURE-ONLY= / =NOT-GRANTED=, hard-refuse root =/=, pass the
  legacy 98-point matrix plus reconciliation/pre-phase/root negatives, strict
  source gates, and selected-live-surface nonmutation, then stop without a
  store build or live action.

** BACKLOG P5.2b-D4b clean-published synthetic fused-program evidence
:PROPERTIES:
:created: 2026-07-19
:helper_published: 002d1ef05bab80162f6807a5cd31b98d209baa7c
:realization_checkpoint: 002d1ef05bab80162f6807a5cd31b98d209baa7c
:first_validator_published: 99dfdeff36443aad4627fb6bb87dd6c42dcb2e87
:second_validator_published: bf8040dfc3ec4438c2f229de60978792ffe0a493
:blocked_by: validated/published third fixture-topology correction and successful recovery validators
:END:

- Helper evidence: [[file:../../../docs/audits/2026-07-19-p5.2b-d4b-helper-source.org][2026-07-19 P5.2b-D4b source-only helper checkpoint]].
- Runtime recovery: [[file:../../../docs/audits/2026-07-19-p5.2b-d4b-runtime-recovery.org][2026-07-19 P5.2b-D4b runtime-validator recovery]].
- [X] Fetch exact published D4a =41e1115= into a clean upstream-equal checkout
  and add a separate source-only exact-input constructor, exact-code loader
  probe, six byte-frozen production-suite paths, checkout-unavailable probe,
  artifact 54+44 restart harness, and commit/live/store-state-bound two-phase
  realization gate.  The pure gate reproduces =95b84e2...= / 956,987 bytes
  and reports =lowered=FALSE= / =realized=FALSE=.
- [X] Publish the helper-only checkpoint at =002d1ef=, fetch it cleanly, and
  consume the exact grant for one unprivileged synthetic =computed-file=
  lowering/realization.  The result is =REALIZED= at
  =/gnu/store/17i7zdd5lhncw5v97cfkxbg4zdvpw5jb-system-pruning-loaded.scm=;
  its SHA256/size equal =95b84e2...= / 956,987, exactly three bounded
  top-level store additions occurred, selected live surfaces remained equal,
  and no result symlink or GC root was created.
- [X] Preserve the first runtime stop: 0-byte stdout SHA256
  =e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855=
  and 222-byte stderr SHA256
  =ea0799970363414fa8c9c5a0d32a2cb6b6125e620abd81f2b926795f26c8864d=.
  The child passed but emitted the exact Guile 3.0.11 warning
  =9100e8a721dc98c18905be24b2c46ad40af5122494a0156aec64003197907a6c=
  / 117 bytes.  Empty stderr remains mandatory: Guile reached =scm_putenv=
  after lazily spawning its finalizer thread and documents multi-thread
  environment mutation as unspecified.  Preserve the warning only as exact
  historical failure evidence.
- [X] Prepare and validate the source-only recovery candidate.  Exact
  loader/runtime/source-gate identities are
  =6591f029...:18049=, =55cab0aa...:57955=, and
  =b86b00ee...:20132=.  The D4b source gate reports
  =lowered=FALSE= / =realized=FALSE=; the strict repository suite, Org lint,
  whitespace checks, and independent review pass with no open critical,
  high, or medium finding.
- [X] Publish first validator checkpoint =99dfdeff= while preserving
  =002d1ef= as the realization/source archive.  Its recovery passed boundary
  and root-backend with empty child stderr, then failed safely at the
  orchestrator whole-line matcher: the frozen suite emitted =PASS (313
  checks)= while the matcher expected bare PASS.  Preserve its 0-byte
  =runtime-recovery.stdout= SHA256
  =e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855=
  and 129-byte =runtime-recovery.stderr= SHA256
  =a29ff114427bca8a4d1bb786a3b77cec9d592b3da2cb37c3d0efbf9f47a670b5=.
  No artifact semantic check failed and no second realization ran.
- [X] Validate and publish second descendant validator checkpoint =bf8040d=
  that
  pins the exact frozen whole-line PASS literal for all six production
  suites.  Published loader/runtime/source-gate identities are
  =6591f029...:18049=, =d4cc2ee0...:61745=, and
  =11fc5047...:20384=.
  Preserve =002d1ef= as the realization/source archive.  Materialize and pin
  only the corrected loader checker from the later checkpoint.  Wrap only
  =load-exact-loader-prefix= in =dynamic-wind= with
  =gc-disable= / =gc-enable=, restore normal GC before packets/suites, and
  have the standalone preexisting-module after thunk restore its saved caller
  module before =gc-enable=.  Require empty stderr at all
  6 + 4 + 1 = 11 checked sites; never classify or allow the old warning.
- [X] Preserve the second recovery stop.  All six production suites passed
  with exact stdout and empty nested-loader stderr before D4a fixture
  verification.  The artifact capability guard correctly rejected the
  harness's 0755 root with exact diagnostic:
  =guix-system-pruning-fused: FAIL: sk-system-pruning-reconciliation ("fixture root is not an exact owned directory")=.
  Preserve 0-byte
  =runtime-recovery-2.stdout= SHA256
  =e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855=
  and 115-byte =runtime-recovery-2.stderr= SHA256
  =fab5d2d47758443c8714f5e5f47cf21158f58dc903675060d11ed6def6462c74=.
  Do not retry =bf8040d= or overwrite any failed channel.  No artifact
  semantic check failed and no second realization or live change ran.
- [ ] Validate and publish a third descendant validator checkpoint that
  separates fixture topology completely.  D4a uses an exact owned 0700 root,
  the exact 0600 reconciliation sentinel, and absent transaction/root
  namespaces.  Legacy 98-point coverage retains the transaction sentinel and
  required directories.  Candidate loader/runtime/source-gate identities are
  =6591f029...:18049=, =dca63d3d...:68082=, and
  =fcacf9c3...:20662=.  Source validation passed; publication is pending.
- [ ] Prove the exact program runs with the checkout unavailable, ignores
  poisoned Guile paths after realization, embeds the reviewed bytes, rejects a
  mutable copy and every live action, and passes the six production-module D4a
  suites plus all 98 interruption points.  Keep the renderer suite as the
  byte-frozen source gate.  The external evidence gate rejects an unrecorded
  alternate store output; the program does not embed its self-referential
  final path/hash.  Run only the corrected runtime and checkout-unavailable
  validators against the retained result/output; report
  =lowering=NOT-RUN= / =realization=NOT-RUN= / =live-action=NOT-GRANTED=.
  Both gates must refuse any realization acceptance token.  Create
  =runtime-recovery-3.stdout= / =runtime-recovery-3.stderr= with no-clobber
  semantics and recheck all three preserved failed-channel pairs before and
  after.
- [ ] Freeze derivation/output identity, program hash/size, loaded-byte
  provenance, raw results, bounded realization effects, and unchanged selected
  live transaction surfaces; publish and stop.

** BACKLOG P5.2b-D4c.1 fresh live-state manifest publication
:PROPERTIES:
:created: 2026-07-19
:blocked_by: published D4b evidence
:END:

- [ ] Recompute every candidate, retained, protected, GRUB/=bootcfg=/EFI,
  transaction-prestate, expected-poststate, candidate/bootcfg root tuple, and
  direct program-root name formula without assuming the historical
  =86/17/5= or 78-candidate snapshot remains current.
- [ ] Publish the closed live-state review manifest with
  =authorization NOT-GRANTED=.  It binds fused-program source inputs but does
  not contain a self-referential future program path/hash or exact
  program-root target tuple.
- [ ] Re-establish clean upstream equality and stop without a program build,
  root, authorization, or transaction mutation.

** BACKLOG P5.2b-D4c.2 exact live fused-program build/evidence
:PROPERTIES:
:created: 2026-07-19
:blocked_by: published D4c.1 manifest
:END:

- [ ] Build one fused program embedding the exact published D4c.1 manifest and
  freeze the non-circular tuple of manifest SHA256, canonical program store
  path, program SHA256/size, and pinned Guix/Guile identities.
- [ ] Repass loader-isolation gates and the read-only live verifier, publish
  the exact evidence, and stop without production roots or a live action.

** BACKLOG P5.2b-D4c.3 unchanged-state execution-packet acceptance
:PROPERTIES:
:created: 2026-07-19
:blocked_by: published D4c.2 evidence and unchanged protected live state
:END:

- [ ] Revalidate repository/GitHub equality, manifest/program identities, boot
  ID, every protected surface, absent transaction/roots, exact selector and
  postflight, and attended quiescence.
- [ ] Accept one exact future-execution packet naming =P5.2b-D5=, the full
  manifest hash, exact program path/hash/size, selector/count transition,
  GRUB/=bootcfg= identities, durable program/candidate/bootcfg roots,
  one-connection temporary roots, the complete ordered bootstrap/lock-retention
  contract, the liveness checks and transaction phases proposed for the later
  grant, and every prohibition.
- [ ] Stop with =execution NOT-GRANTED=.  Packet acceptance is not an execution
  grant, and any later drift invalidates it.

** BACKLOG P5.2b-D5 attended System transaction and postflight
:PROPERTIES:
:created: 2026-07-19
:blocked_by: accepted D4c.3 packet; initial apply requires unchanged final preflight/grant; recovery requires one accepted transition and fresh continuation grant
:END:

- [ ] Initial apply: require the exact absent D4c.3 transaction/root/lock
  prestate, repeat the full identity/protected-state/quiescence preflight, and
  obtain a separate exact D5 execution grant before any effect.
- [ ] After that grant, open one store connection, temp-pin the exact fused
  program as the first daemon mutation, then atomically create and prove
  =/var/guix/gcroots/p52b-system-prune-program-MANIFEST-SHA256= as the first
  persistent mutation and durable GC root.
- [ ] Only after the program root is proven, create/validate transaction-base
  and persistent lock files, acquire transaction then System lock without
  waiting, revalidate under both, then temp-pin and create the ordered
  candidate/bootcfg roots and remaining declared bootstrap prefixes.
- [ ] Reconcile/recover: instead of requiring absent prestate, require one
  exact program-root-first manifest-declared transitional state, legal journal
  head when present, exact phase-specific protected state, fresh attended
  re-attestation, and a separate continuation grant naming that state and next
  phase.  Reacquire both locks and revalidate under them before continuing.
- [ ] Prove exact =valid-path?=, =find-roots=, and authorized =live-paths=
  results before any GRUB, =bootcfg=, or generation-link mutation; do not call
  =add-indirect-root= for direct roots.  Use =live-paths= only for closed
  target-membership checks; do not treat its broader returned set as evidence.
- [ ] Keep the fused-program durable root through durable =COMPLETE= or
  =ROLLED-BACK= plus terminal postflight, and remove it last while its temporary
  root remains live; revalidate before every forward, rollback, and cleanup
  phase.  Retain the exact transaction/System lock inodes in terminal
  postflight; never unlink their persistent pathnames.
- [ ] Freeze exact postflight and stop without GC, collection, activation,
  reconfiguration, bootloader installation, EFI mutation, reboot,
  =guix gc --list-live=, =guix gc --list-dead=, or any unreviewed liveness
  query.

* BACKLOG P5.3 reviewed unreachable-store collection
:PROPERTIES:
:created: 2026-07-18
:blocked_by: completed D5 with installed GRUB aligned to retained System links, accepted post-P5.2 non-mutating estimate, and separate explicit collection authorization
:END:

- Treat generation-link deletion and store-object collection as different
  destructive operations.
- P5.2a ran no GC or dead/live query, removed no top-level store object, and
  reclaimed zero material store space.
- P5.2b-D1 ran no deletion or collection.  Its read-only forward-reference
  review shows that the current =bootcfg= is not recovery coverage for every
  old System closure.
- Review the exact post-deletion liveness boundary before separately
  authorizing any query with side effects or any collection.

* TODO Hardware inventory for custom kernel
:PROPERTIES:
:created: 2026-07-03
:END:
Collect hardware facts after the base machine setup is stable. Do not start a custom kernel before this inventory exists.

Commands to capture:

#+begin_src sh
mkdir -p ~/.config/guix/machines/guixpc

{
  echo "===== uname ====="
  uname -a

  echo
  echo "===== guix describe ====="
  guix describe

  echo
  echo "===== lspci -nnk ====="
  lspci -nnk

  echo
  echo "===== lsusb ====="
  lsusb

  echo
  echo "===== lsmod ====="
  lsmod

  echo
  echo "===== network sysfs ====="
  find /sys/class/net -maxdepth 2 -type l -print

  echo
  echo "===== drm sysfs ====="
  find /sys/class/drm -maxdepth 2 -type l -print

  echo
  echo "===== firmware/kernel messages ====="
  dmesg | grep -Ei 'firmware|wifi|wlan|bluetooth|amdgpu|nvme|ahci|usb|realtek|mediatek|intel'
} > ~/.config/guix/machines/guixpc/hardware-inventory.txt
#+end_src

* TODO Custom kernel plan
:PROPERTIES:
:created: 2026-07-03
:END:
Only after hardware inventory is reviewed:

- Identify required boot, storage, and filesystem modules.
- Identify exact Realtek Wi-Fi chipset, driver, and firmware.
- Identify AMD graphics path and firmware needs. Do not use the failed
  =amdgpu= initrd preload approach as the baseline.
- Decide whether to pin a known-good kernel or track Nonguix linux.
- Build custom kernel as a separate generation, keeping the generic Nonguix generation as rollback.
