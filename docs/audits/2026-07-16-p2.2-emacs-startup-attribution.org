#+title: GuixPC P2.2 Emacs Startup Attribution
#+date: 2026-07-16
#+status: complete

* Outcome and boundary

P2.2 records one fresh Emacs/EXWM startup before any performance policy is
changed.  It owns attribution only: a versioned, opt-in observer recorded named
synchronous boundaries in memory, and a guarded client extracted the completed
trace to stdout after the desktop was running.

No GC setting, eager/deferred decision, package, module order, cache, native
compilation policy, message advice, launcher path, EXWM behavior, Home profile,
System profile, or kernel setting changes in this slice.  A later P2.3 may test
at most one replicated, user-relevant hypothesis.

Rafael judged R1's 8.712893-second instrumented Emacs/EXWM session-start window
not materially disruptive in normal use because Emacs starts once during boot.
P2.2 therefore closes with no tuning target or configuration change.  P2.3
remains =BACKLOG= and unselected.

Related records:

- [[file:2026-07-15-p2.1-emacs-exwm-measurement.org][P2.1's accepted performance baseline]];
- [[file:2026-07-15-phase-0-baseline.org][Phase 0 and P1.1 baseline]];
- [[file:2026-07-12-full-configuration-audit.org][completed numbered-slice audit]];
- [[file:~/Documents/OrgFiles/Topics/2026/2026-07-14-guix-polishing-and-development.org][editable agenda roadmap]].

* What “blame” can mean here

=Blame= is useful conversational shorthand, but the evidence uses
=attribution=.  An attributed row is inclusive elapsed wall time observed
between timestamps immediately around one named synchronous boundary in one
instrumented process.

For a module, the boundary is the outer top-level =require= from =init.el=.
It includes any nested synchronous requirements, autoload work, file/native
code loading, top-level configuration, hooks or advice invoked before return,
GC that happens inside the interval, I/O, and scheduler delay.  It is not:

- exclusive CPU time or source-line ownership;
- proof of causal responsibility or user-visible harm;
- the amount removable by deferring or deleting the module;
- an expected saving;
- a stable ranking from one observation.

Chronological phase rows are primary.  A ranked module view is secondary and
is called the “largest observed inclusive boundary”, never the slowest module
or the cause.  Percentages use the named measured parent window.  Gaps remain
an explicit residual instead of being forced onto a module.

* Source-confirmed startup process

** Before Emacs

The graphical path is:

#+begin_src text
tty1 Fish policy
  -> sk-start-exwm
  -> scripts/exwm-session
  -> startx
  -> ~/.xinitrc -> shell/xinitrc
  -> exec emacs -fs --eval '(load-file "~/.exwm")'
#+end_src

Before =exec emacs=, =shell/xinitrc= synchronously:

1. sources =/etc/profile= and =~/.profile=;
2. validates the canonical Guix Home D-Bus address and waits at most five
   seconds for its socket;
3. updates the graphical D-Bus activation environment;
4. applies X blanking/DPMS policy with =xset=;
5. queries XRandR and applies the 3440x1440/100 Hz fallback if necessary;
6. sets the wallpaper synchronously;
7. stops the prior Picom instance and backgrounds the replacement.

These are confirmed owners in the login path, but P2.1's 8.742822-second value
contains none of them.  P2.2 maps them as an external path and does not invent
timestamps for work that the current session did not record.  Xorg readiness,
boot-to-login, and a full pre-Emacs critical-path trace remain separate P2.S1
work.

** What the 8.742822 seconds actually contains

The active Emacs 30.2 source sets =before-init-time= at the start of Lisp
startup.  Its =command-line= then loads =early-init.el=, performs package and
window-system initialization, creates the initial X frame, loads =init.el=,
and sets =after-init-time=.  =emacs-init-time= is the difference between those
two timestamps.

Only afterward does Emacs run =after-init-hook= and process remaining command
arguments, including this session's =--eval '(load-file "~/.exwm")'=.  The
source order proves that the P2.1 value includes Emacs bootstrap, early init,
package/window/frame setup, and user init, but excludes:

- =after-init-hook=;
- =~/.exwm= and =emacs/exwm-loader.el=;
- =require sk-exwm= and =sk/exwm-start=;
- =exwm-wm-mode= activation;
- =initial-buffer-choice= dashboard construction;
- =emacs-startup-hook= and =window-setup-hook=;
- first visible frame, first managed X client, and first usable input.

It is therefore an Emacs initialization value, not an EXWM-ready or perceived
desktop-ready value.

** Direct init boundaries

=init.el= directly requires =use-package=, then these 23 personal features in
exact order:

#+begin_src text
sk-core sk-ui sk-windows sk-dired sk-terminal sk-dashboard sk-completion
sk-evil sk-project sk-lsp sk-lisp sk-clojure sk-racket sk-fennel sk-lua
sk-python sk-shell sk-json sk-c sk-format sk-keys sk-org sk-notes
#+end_src

=sk-window-policy= is pulled transitively before its later direct consumer, and
=sk-exwm= is loaded through the post-init EXWM bridge.  Thus all 25 repository
personal modules have evaluated by =sk/exwm-start=, but only 23 are direct
=init.el= boundaries.

There are 42 top-level =use-package= declarations across the personal modules.
That is not an eager-package count.  Lazy keywords, dumped features,
transitive dependencies, native code, and package configuration make a static
count unsuitable for timing attribution.

** Confirmed synchronous candidates, not measured costs

Source inspection confirms work inside the initialization boundary including:

- profile/native-cache identity and directory setup in early init;
- cache directories, persistent modes, and server setup in =sk-core=;
- font, theme, and modeline setup in =sk-ui=;
- a real =ls= process during Dired configuration;
- eager Yasnippet reload in =sk-completion=;
- Magit/Counsel/Projectile startup edges in =sk-project=;
- explicit Flycheck loading and after-init setup in =sk-lsp=;
- five demanded dialect frontends;
- Org, six Babel backends, habit, and tempo setup;
- recursive agenda discovery and inbox setup in =sk-notes=.

This list identifies where work is owned.  It cannot assign any share of
8.742822 seconds without a fresh trace.

* Protocol v1 design

** Activation and observer lifetime

Ordinary startup checks one exact environment value and otherwise does not load
the observer:

#+begin_src text
SK_EMACS_STARTUP_TRACE=p2.2-v1
#+end_src

When present, =early-init.el= loads =sk-startup-trace.el= before the native
cache body.  The observer:

- stores events only in =sk/startup-trace-events=;
- installs a temporary =require= advice that records only selected outermost
  direct calls while preserving their order, return values, and errors;
- temporarily advises dashboard construction and =exwm-wm-mode= after their
  defining features load;
- adds tail markers to =after-init-hook=, =emacs-startup-hook=, and
  =window-setup-hook=;
- seals the trace at the configured window-setup tail and removes every
  observer hook and advice.

The observer writes no file, emits no measurement message, starts no process,
opens no UI, forces no GC, and changes no GC/cache/loading policy.  Its own
loading, advice, timestamps, list allocation, and extraction-related retained
objects are observer overhead.  That overhead is included and is not
subtractable.

** Recorded boundaries

The raw trace owns these classes:

| Class | Boundaries |
|-------+------------|
| marks | early-init enter/exit; init enter/exit; after-init hook tail; EXWM loader/config enter/exit; startup hook tail; window-setup tail; trace complete |
| direct requirements | =use-package=; 23 =init.el= personal features; later =sk-exwm= |
| named calls | observer bootstrap; =~/.emacs.d/exwm.el= load; =sk/exwm-start=; =exwm-wm-mode=; initial dashboard construction |

Each span retains raw start/end/elapsed seconds, completion status,
already-loaded state where applicable, and GC count/time delta.  A GC delta is
temporal co-occurrence, not proof that GC caused the interval.

The collector derives non-overlapping phases from =before-init-time= through
the final trace marker and reports:

- the complete init-file window;
- all 24 direct init requirements chronologically;
- the 23-personal-module window and sum;
- residual time between personal-module boundaries;
- the EXWM/dashboard/startup bridge without adding nested inclusive spans;
- a secondary duration ranking for the 23 direct personal requirements.

** Extraction boundary

After the attributed desktop settles, run from a separate SSH connection:

#+begin_src sh
cd ~/Projects/guix-dotfiles
scripts/emacs-startup-attribution
#+end_src

The collector selects the exact active-Home =emacsclient=, reads the sealed
trace twice, and emits versioned TSV to stdout only after validation.  It
fails closed for dirty/unsynchronized source, profile or source mismatch,
inactive EXWM/server, incomplete observer cleanup, malformed/nonfinite/
negative times, missing/duplicate/out-of-order boundaries, wrong feature
order, overlapping direct requirements, inconsistent elapsed values, or phase
arithmetic that does not close within timer precision.

Live provenance includes the repository commit, boot ID, kernel, exact Home
profile/client/server, native-cache identity, Emacs version/PID/start time,
timer identity, activation token, GC policy, and observer event count.

** Activation/extraction attempts and source corrections

Initial implementation checkpoint =a2a6283= added the dormant observer,
collector, and fixtures.  The first three 2026-07-16 attempts are retained as
rejected process evidence; none authorizes P2.2 attribution.  A fourth fresh
process from the twice-corrected, published source produced accepted trace R1.

1. At =2026-07-16T11:37:48Z= the session runner was invoked from an SSH
   pseudo-terminal.  Guix's generated =startx= rejected fd 0 as
   =/dev/pts/0= because it could not determine a physical VT, and returned
   status 1 before X or Emacs startup.  The original tty1 EXWM session stayed
   alive.  The preserved failure log is
   =/tmp/p2.2-ssh-pts-start-failure.log= with SHA-256
   =b9e6485e3ab9296be11ddd5ca290cbc0d872c7fdada3423b3eb0e5b0d9c3058f=.
2. A controlled remote handoff then exported the exact token temporarily as a
   Fish universal variable, ended the old EXWM process cleanly, and recycled
   only the idle tty1 Fish shell.  Respawned mingetty autologin followed the
   canonical local tty1 -> Fish -> session runner -> =startx= path.  The new
   Emacs inherited =p2.2-v1=, but =early-init.el= failed before installing the
   observer.  The universal variable was erased immediately; the collector
   was not run.
3. After publishing source-path correction =e2ed5b5=, a fresh tty1 Emacs
   sealed all 42 expected events with no init error.  At
   =2026-07-16T12:01:25Z= the collector was invoked exactly once, returned
   status 1, emitted a zero-byte TSV, and wrote 99 bytes of stderr:
   =startup events are not chronologically sorted at
   mark:exwm-config-enter=.  The empty TSV SHA-256 is
   =e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855=;
   the stderr SHA-256 is
   =e7c7188e26377490d4cab5ff5031dbccdc56eed3fad60e6d9eb8628a6d2e29f2=.
   The collector was not rerun against that process.
4. After publishing numeric correction =ba087f5=, a new canonical tty1 session
   started Emacs PID 6631 at =2026-07-16T12:09:47Z=.  It sealed all 42 events,
   reported no init error, and removed all observer advice and hooks.  The
   collector was invoked once at =2026-07-16T12:11:42Z= against the clean,
   synchronized =ba087f5= checkout.  It returned status 0, emitted the accepted
   139-line TSV to stdout, and emitted zero bytes to stderr.

The second attempt exposed a confirmed source-path bug, corrected by
=e2ed5b5=.  The managed live
=~/.emacs.d/early-init.el= is a symlink into this repository, while the
observer was located relative to =user-emacs-directory=.  That expanded to
the nonexistent =~/.emacs.d/lisp/sk-startup-trace.el= and set
=init-file-had-error=.  The correction resolves =load-file-name= through
=file-truename= and locates =lisp/= beside the repository's true
=early-init.el= source.  A new isolated fixture reproduces the live symlink
entrypoint with no =~/.emacs.d/lisp/= fallback; it must load both the observer
and the remainder of native-cache setup.

The third attempt exposed a collector-only numeric bug, corrected by
=ba087f5=, not an event-order defect.  The chronological guard used a =1e-9=
relative tolerance on absolute
Unix timestamps near =1.784e9= seconds.  It therefore treated timestamps up to
roughly 1.78 seconds apart as equal.  The outer EXWM load and its inner
=exwm-config-enter= mark began less than one millisecond apart; after being
misclassified as simultaneous, their intentionally nested end times appeared
reversed.  Absolute clock readings now use only the collector's one-microsecond
timer precision, while elapsed durations retain their separate numeric
tolerance.  An epoch-scale nested-span fixture reproduces the live timestamp
magnitude.  Because this correction changes the collector after process
startup, the sealed process is rejected rather than attributed to a later
repository commit; a new process is required after publishing the correction.

The SSH attempt also demonstrated a separate launcher-hygiene issue: calling
=scripts/exwm-session= while an EXWM session is already active rotates the
bounded log pair before =startx= rejects the new caller.  The live writer
continued safely on the renamed inode and its pre-trace log was preserved as
=/tmp/p2.2-pretrace-live-session.log= with SHA-256
=ce5703576f321f8c49935d624caa0bc00bcbbe0c692c4c52256a2250936099cc=.
Adding a direct-run VT/session preflight belongs to a later launcher-hygiene
slice; it is not startup timing evidence and is not folded into P2.2.

* Accepted trace R1

** Artifact and provenance

The accepted raw and derived record is
[[file:data/2026-07-16-p2.2-emacs-startup-attribution.tsv][=data/2026-07-16-p2.2-emacs-startup-attribution.tsv=]].  The externally
preserved source artifact and the tracked copy are byte-identical:

#+begin_src text
SHA-256: 02d78dce100f5eb5abd27a86ac94750e88ee1d40ea9b3c8fa434e8e106fa50e4
Lines:   139
Bytes:   9823
stderr:  0 bytes
stderr SHA-256: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
#+end_src

The collector itself validated twelve invariants, all =PASS=: repository and
upstream stability; system and profile stability; boot identity; live source
and server provenance; activation and completeness; protected trace state;
event schema and order; non-overlapping phase partition; complete direct init
requirements; nonnegative module residual; attributed EXWM bridge; and
stdout-only collection.

Live provenance retained by the artifact is:

| Field | Accepted value |
|-------+----------------|
| capture UTC | =2026-07-16T12:11:42Z= |
| host / kernel | =guixpc= / =7.0.14= |
| boot ID | =6a94d3ec-1c03-40cd-a678-d8a372bf7df2= |
| repository HEAD / upstream | =ba087f51c3ee4fd53e8885335ee21df615c36daf= / same |
| ahead / behind | =0/0= |
| booted / current System | =/gnu/store/gz53dagd09rr97k0ffhkx40rz0am4c88-system= / same |
| Home profile | =/gnu/store/bwa6glmgygb8fvw5yj4fvfvk85rqsgdc-profile= |
| Pull profile | =/gnu/store/mm52g4iy2hx36vn27h7y13cgc8zqzv5c-profile= |
| Emacs | =30.2=, PID =6631=, started =2026-07-16T12:09:47Z= |
| running executable | =/gnu/store/jbqsprk039wqdb53ljqw9rhm9wg27f5v-emacs-30.2/bin/emacs= |
| activation / timer | =p2.2-v1= / =emacs-current-time-realtime= |
| observer state | complete, 42 events, all temporary advice/hooks removed |
| GC policy | threshold =800000=; percentage =0.100000= |

The exact live source root was
=/home/skydive420dz/Projects/guix-dotfiles/emacs/=.  EXWM was loaded,
connected, and in =exwm-wm-mode=; the server was running.  The trace remained
sealed in PID 6631 after extraction.  These facts establish the provenance of
R1; they do not establish cold/warm cache state or perceived readiness.

** Non-overlapping phase map

The accepted chronological phase partition, in seconds, is:

| Phase | Observed seconds |
|-------+-----------------:|
| Emacs before early init | 0.007820 |
| early init | 0.252701 |
| Emacs core before init file | 0.709003 |
| init file | 6.926169 |
| Emacs core after init file | 0.024163 |
| after-init hooks | 0.092080 |
| command line before EXWM loader | 0.006269 |
| EXWM loader before config | 0.001382 |
| EXWM config | 0.655097 |
| EXWM loader after config | 0.000862 |
| command line after EXWM loader | 0.036933 |
| startup hook to window-setup hook | 0.000224 |
| trace finalization | 0.000190 |

=emacs-init-time= observed =7.919856= seconds from =before-init-time= to
=after-init-time=.  The complete recorded partition from =before-init-time= to
the trace-complete mark observed =8.712893= seconds.  The latter marker is not
a first-visible, first-client, or first-usable-input measurement.

Within the init file, the =use-package= boundary observed =0.039544= seconds;
all 24 direct require boundaries summed to =6.869371= seconds; and the explicit
init-file residual was =0.056798= seconds.  The 23-personal-module window was
=6.886196= seconds, its inclusive direct-require boundaries summed to
=6.829827= seconds, and its residual was =0.056369= seconds.

Inside the later bridge, the =sk-exwm= inclusive require boundary observed
=0.615709= seconds.  =sk/exwm-start= observed =0.038688= seconds and contains
the nested =exwm-wm-mode= boundary of =0.015712= seconds.  Dashboard
construction observed =0.024341= seconds after the EXWM loader.  Nested spans
must not be added to the non-overlapping =0.655097=-second EXWM-config phase.

** Direct personal-require boundaries

The secondary ranking below is exact for R1.  “Percent” uses the
=6.886196=-second direct-personal-module window.  Every value is inclusive and
may contain nested dependency, GC, I/O, advice, and scheduler work.

| Rank | Inclusive boundary | Observed seconds | Percent |
|------+--------------------+-----------------:|--------:|
| 1 | =sk-racket= | 1.316815 | 19.122537 |
| 2 | =sk-project= | 1.233377 | 17.910854 |
| 3 | =sk-notes= | 1.035355 | 15.035221 |
| 4 | =sk-lisp= | 0.768023 | 11.153085 |
| 5 | =sk-core= | 0.603429 | 8.762875 |
| 6 | =sk-evil= | 0.541546 | 7.864230 |
| 7 | =sk-ui= | 0.494968 | 7.187828 |
| 8 | =sk-terminal= | 0.287120 | 4.169506 |
| 9 | =sk-completion= | 0.193146 | 2.804826 |
| 10 | =sk-clojure= | 0.102843 | 1.493471 |
| 11 | =sk-lsp= | 0.075565 | 1.097336 |
| 12 | =sk-dired= | 0.051900 | 0.753684 |
| 13 | =sk-windows= | 0.048624 | 0.706106 |
| 14 | =sk-fennel= | 0.035852 | 0.520642 |
| 15 | =sk-keys= | 0.019816 | 0.287771 |
| 16 | =sk-dashboard= | 0.009950 | 0.144495 |
| 17 | =sk-json= | 0.003695 | 0.053656 |
| 18 | =sk-lua= | 0.003639 | 0.052850 |
| 19 | =sk-org= | 0.002158 | 0.031334 |
| 20 | =sk-format= | 0.000546 | 0.007922 |
| 21 | =sk-python= | 0.000500 | 0.007255 |
| 22 | =sk-shell= | 0.000488 | 0.007083 |
| 23 | =sk-c= | 0.000472 | 0.006849 |

Thus =sk-racket= is the largest observed inclusive personal-require boundary
in R1.  R1 alone does not show that Racket configuration caused a user-felt
delay, that the ordering will repeat, or that changing it would save time.

** Post-extraction gates

- =scripts/emacs-check=: =PASS=.  The live check passed with Org 9.8.7 and the
  profile-keyed native cache; all 59 ERT tests, batch checks, Geiser/Guile,
  SLY/SBCL, and backend checks passed, and the repository remained unchanged.
- =scripts/desktop-session-check=: =PASS=.  The canonical D-Bus and EXWM
  checks passed along with PipeWire, RTKit, and Picom.  The Bose Bluetooth sink
  was the physical default, and the finite low-volume stream check passed.
- =scripts/shell-check --strict=: =PASS=.  This includes
  =emacs-startup-attribution-check= and the strict Guile fixtures.
- Final repository state was empty/clean at
  =ba087f51c3ee4fd53e8885335ee21df615c36daf= with upstream at the same commit
  and =0/0= ahead/behind.  Live PID 6631 still held a sealed 42-event trace;
  every observer cleanup probe remained nil.

* Truth boundaries

- One fresh trace is an =OBSERVED= natural-state sample, not a distribution,
  stable rank, cold start, or warm start.
- Emacs =current-time= is a realtime wall clock.  The collector rejects
  negative or internally inconsistent values, but this is not a monotonic
  timer and a clock adjustment remains a run-invalidating risk.
- Natural Guix/native/filesystem caches are uncontrolled and are not reset.
- Inclusive module time may contain dependency, GC, I/O, advice, or scheduler
  work; asynchronous work after return is outside that module.
- The configured hook-tail markers are exact for the current source-owned hook
  order, not universal Emacs internals after arbitrary future hooks.
- First visible frame, first EXWM-managed client, and first usable input remain
  =UNMEASURED=.  Rafael's qualitative no-disruption judgment is acceptance
  evidence, not a new timestamp or timing invariant.
- =after-init-time= and the later EXWM bridge have different boundaries.  Do
  not add or compare them as if both were =emacs-init-time=.
- A large row does not prove harm, causality, removability, repeatability, or
  expected savings.  Replication is required before P2.3.

* Accepted implementation scope

- [X] Map the complete source-owned path and establish the P2.1 timestamp
  boundary from active Emacs 30.2 source.
- [X] Define attribution language that separates inclusive association from
  causality.
- [X] Add the exact opt-in in-memory observer and cleanup boundary.
- [X] Add a read-only, stdout-only guarded collector and versioned TSV schema.
- [X] Add deterministic clock, order, error-propagation, cleanup, payload, phase
  algebra, residual, malformed/incomplete, and provenance fixtures.
- [X] Pass reader, byte-compile, Checkdoc, ERT, strict shell, candidate Emacs,
  and desktop source gates.
- [X] Publish implementation =a2a6283= and both bounded corrections =e2ed5b5=
  and =ba087f5= before accepting a trace from their source.
- [X] Pair with Rafael for one accepted fresh attributed EXWM process after
  retaining all three rejected attempts.
- [X] Preserve the first valid output externally with empty stderr and reviewed
  SHA-256, then stage its byte-identical copy for the evidence commit.
- [X] Run live editor/desktop/source gates after extraction.
- [X] Review with Rafael whether the observed startup is materially felt.  He
  reported no disruptive behavior in normal use because Emacs starts once
  during boot; P2.2 selects no tuning target and P2.3 remains unselected.

* Non-goals and stop conditions

- No lazy-loading conversion, module reorder/removal, GC threshold, message
  advice, native-comp, package, Org, launcher, dashboard, EXWM, or visual change.
- No profiler, ELP, esup, benchmark macro, forced GC, cache clearing, repeated
  cosmetic run, or reload substituted for a fresh process.
- No Home/System activation, reboot, application launch, generation cleanup,
  privilege, boot/kernel/IRQ/TSC/HPET, build, thermal, or storage experiment.
- No first-ready claim from the final hook marker.

Stop and split the work if observation requires behavioral refactoring,
persistent normal-session overhead, a cache reset, privilege, or an invented
readiness marker.  A startup error must propagate normally; the observer may
record it but must never swallow it.

* Paired activation and recovery

The implementation must first be committed, pushed, present in the clean live
checkout, and pass candidate checks.  Activation is a fresh Emacs/EXWM process,
not a config reload.  It is not authorized merely by committing this dormant
observer.

When Rafael is ready and all graphical work is saved:

1. exit the current Emacs normally so =scripts/exwm-session= returns to tty1;
2. from the tty1 Fish prompt run exactly:

   #+begin_src sh
   env SK_EMACS_STARTUP_TRACE=p2.2-v1 \
     ~/Projects/guix-dotfiles/scripts/exwm-session
   #+end_src

3. leave that fresh graphical session running and collect through SSH;
4. preserve the first valid external TSV; do not restart for a prettier value.

If the attributed session fails, return to a tty, inspect
=~/.local/state/sk-exwm/startx-current.log=, and run the same session script
without the environment variable.  The observer is dormant without the exact
token.  Source rollback is to revert the implementation checkpoint before a
later normal restart; no Home/System generation rollback is involved.

* Decision rule and successor

P2.2 closes with an attribution map and no tuning change.  Good evidence wording
for the accepted observation is:

#+begin_quote
In trace R1, the inclusive =sk-racket= require boundary observed 1.316815
seconds (19.122537 percent of the direct-personal-module window), including
synchronous dependency and GC work; causality and repeatability are untested.
#+end_quote

Do not write “=sk-racket= caused 1.316815 seconds”, “=sk-racket= is slow”, or
“changing it will save 1.316815 seconds”.

Rafael reported no material disruption from the once-per-boot startup, so P2.3
remains =BACKLOG= and unselected.  Promote it only if a later concrete user-felt
complaint emerges; then replicate the same protocol, pair at most one boundary
with that complaint, and test one reversible hypothesis.

* Evidence

Accepted implementation and correction chain:

#+begin_src text
a2a6283  emacs: add one-shot startup attribution
e2ed5b5  emacs: fix traced early-init source path
ba087f5  performance: fix epoch-scale trace validation
#+end_src

Trace R1 was collected from published =ba087f5= on 2026-07-16.  Its tracked
artifact, digest, provenance, exact phase partition, inclusive-boundary map,
collector invariants, rejected attempts, and post-extraction gates are recorded
above.  No configuration tuning, Home/System activation, or reboot was part of
P2.2.  Rafael completed the qualitative review on 2026-07-16 and reported no
material disruption in normal use.  P2.2 is complete without a configuration
change, and P2.3 remains unselected.
