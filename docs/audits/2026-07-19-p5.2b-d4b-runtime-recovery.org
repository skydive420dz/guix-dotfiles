#+title: P5.2b-D4b fused-artifact runtime-validator recovery
#+author: Rafael Oliveira
#+date: 2026-07-19
#+startup: overview
#+category: guixpc
#+filetags: :guixpc:guix:scheme:runtime:recovery:evidence:
#+status: D4b evidence complete; published at 9577e1d; D4c.1a next

* Outcome

The one explicitly authorized P5.2b-D4b lowering and
realization-or-confirmation succeeded from published helper checkpoint
=002d1ef05bab80162f6807a5cd31b98d209baa7c=.  It produced the exact
956,987-byte D4a fused program, classified every bounded store addition,
preserved the selected live surfaces, and created no result symlink or GC
root.

The first runtime gate then stopped at its first nested production-suite
loader because the child emitted a Guile 3.0.11 environment-mutation warning.
The child exited successfully and printed both required PASS lines, but empty
=stderr= was and remains the correct gate.  Guile documents concurrent
environment mutation as unspecified; the exact warning must remain fatal.
The defect is in the validator-owned loader checker's GC/thread timing, not
the realized artifact.

The realized program and its closed result remain the accepted D4b artifact
inputs.  The first source-only correction was published at
=99dfdeff36443aad4627fb6bb87dd6c42dcb2e87=.  Its recovery run passed the
boundary and root-backend production suites with empty child stderr, then
stopped safely at the orchestrator suite because the validator's whole-line
matcher expected a bare PASS while the frozen suite emits =PASS (313
checks)=.  No artifact semantic check failed.

That second stop is preserved as validator evidence.  Its correction was again
source-only: it pinned the six exact frozen suite PASS literals rather than
changing a suite or the artifact.  At that stop it still required validation
and publication at another descendant checkpoint before it could run.  It
could not lower, build, realize, or confirm another derivation and could
perform no live action.

That correction was published at
=bf8040dfc3ec4438c2f229de60978792ffe0a493=.  Its second recovery passed all
six exact production suites with empty nested-loader stderr, then stopped
safely when the artifact's D4a fixture-capability guard rejected the
validator harness's 0755 root.  The artifact guard behaved correctly; the
harness had reused legacy transaction-fixture topology where the D4a path
requires a distinct 0700 root, exact reconciliation sentinel, and absent
transaction/root namespaces.

This second recovery stop is also preserved as validator evidence.  No
artifact semantic check failed, no failed channel was overwritten, and no
second lowering, build, realization, confirmation, or live change occurred.
The source-only harness correction was later published with clean equality at
=cdd4bd489313f36c325ea0841ad7586b6d152904=.  Its third recovery completed
the exact runtime-v4 and checkout-independence gates.  The bounded runtime
proof is complete, and its docs/data evidence is published with clean upstream
equality at =9577e1daac3d5258222d1cbf186facba809d10fb=.  D4c.1a production
source/schema/fixture work is now eligible; D4c.1b live-state capture remains
later.

* Retained realization

The consumed authorization covered exactly one unprivileged
=computed-file= lowering and realization-or-confirmation with substitutes,
grafts, offload, result/GC roots, and live action disabled.

| Item | Exact identity |
|------+----------------|
| Realization checkpoint | =002d1ef05bab80162f6807a5cd31b98d209baa7c= |
| D4a rendered source SHA256 / bytes | =95b84e29853a2327bffab857383cf78a30ab41b965144fd298edc384335b9d70= / =956,987= |
| Derivation | =/gnu/store/3ky2sgbkg10amsvfx5as4m2w76szkr6a-system-pruning-loaded.scm.drv= |
| Output | =/gnu/store/17i7zdd5lhncw5v97cfkxbg4zdvpw5jb-system-pruning-loaded.scm= |
| Result state | =REALIZED= |
| Realization result | =/tmp/p52b-d4b-realization-evidence.002d1ef05bab80162f6807a5cd31b98d209baa7c/realization-result.tsv= |
| Result SHA256 / bytes | =02506b54d887da9e2c10a94a62a2527f3a1b128380c628f3efa2efeb2e445fd3= / =914= |
| Preflight snapshot | =6a2c9073ff890df220dd619b373ef7e5e428c2a1ddc20de8f4692f3bb3025bd7= |
| Top-level store additions | =3= |
| Unrelated additions / removals | =0 / 0= |
| Selected live surfaces | byte-identical |
| Result symlink / GC root | absent / none created |
| Live action | =NOT-GRANTED= |

Every phase status in the closed result is zero and its terminal row is PASS.
The output SHA256 and size equal the independently frozen D4a render.  These
facts remain bound to the realization checkpoint; a later validator commit
must not rewrite their provenance.

* First runtime stop

The published runtime helper at the realization checkpoint has historical
identity:

| File | Bytes | SHA256 |
|------+-------+--------|
| =tests/guix-system-pruning-d4b-runtime-check= | 46,151 | =bfd4cfb6a51f4651cee9fd03073d817a7e3c8dccc9a92ce9a1f3990ddc7d2a28= |

Its first post-realization invocation exited 1 before entering the complete
runtime matrix.  The original raw channels are retained and must not be
overwritten:

| Channel | Bytes | SHA256 |
|---------+-------+--------|
| =runtime.stdout= | 0 | =e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855= |
| =runtime.stderr= | 222 | =ea0799970363414fa8c9c5a0d32a2cb6b6125e620abd81f2b926795f26c8864d= |

The exact failed top-level diagnostic was:

#+begin_example
warning: mutating the process environment while multiple threads are running;
         further behavior unspecified.
guix-system-pruning-d4b-runtime-check: FAIL: exact-production module suite emitted diagnostics: boundary
#+end_example

The child warning alone is exactly 117 bytes:

| Item | Value |
|------+-------|
| Warning SHA256 | =9100e8a721dc98c18905be24b2c46ad40af5122494a0156aec64003197907a6c= |
| Warning bytes | =117= |

The exact production-boundary child exited zero and printed its required
suite PASS plus loader PASS.  Repetition and the isolated canary modes showed
the same warning bytes, but repeatability does not make the condition safe.

The validator-owned checker allowed Guile's finalizer thread to be spawned
lazily before the exact startup prefix completed.  A startup =setenv= then
reached Guile 3.0.11's =scm_putenv= with multiple threads present.  Guile
warned because mutation of the process environment in that state has
unspecified behavior.  Therefore the historical 117-byte warning is retained
as exact failure evidence; it must not be classified, allowlisted, suppressed,
or accepted by the recovery gate.

* First bounded validator correction contract

The corrected runtime validator must preserve two independent checkpoints:

1. =realization-checkpoint= is the historical
   =002d1ef05bab80162f6807a5cd31b98d209baa7c= checkpoint that constructed
   and realized the accepted artifact.
2. =validator-checkpoint= is the later clean, published descendant
   =99dfdeff36443aad4627fb6bb87dd6c42dcb2e87= containing only the reviewed
   loader-checker/runtime-harness recovery correction and its documentation.

The validator must export production suites, fixtures, manifests, and every
artifact input from the realization checkpoint, not from the later validator
checkpoint.  The corrected validator-owned loader checker is the one
deliberate exception: it must be materialized from the exact later validator
checkpoint, made unwritable in the private runtime tree, and pinned by exact
SHA256 and size.  The gate must separately require clean
=HEAD == origin/main == validator-checkpoint= and prove that the realization
checkpoint is its ancestor.

The corrected loader checker wraps only =load-exact-loader-prefix= in:

#+begin_src scheme
(dynamic-wind
  gc-disable
  (lambda ()
    (load-exact-loader-prefix %fused-program))
  gc-enable)
#+end_src

The =dynamic-wind= after thunk restores normal GC even if prefix loading
fails.  Normal GC must be restored before any source packet, production
suite, or canary is evaluated; the correction is not a whole-process GC
suppression.

The standalone preexisting-module refusal must save its caller module before
loading the store program.  Its =dynamic-wind= after thunk restores that
caller module first and calls =gc-enable= second.  This preserves both module
context and normal collection across the expected prefix refusal.

All nested-loader sites still require empty =stderr=.  The expected closed
count is 11 checked sites:

| Site family | Count |
|-------------+-------|
| Six byte-frozen production suites | 6 |
| Four isolated loader-canary modes | 4 |
| Direct preexisting-module refusal | 1 |
| Total | 11 |

At every site, successful child status, the exact expected PASS lines, and a
direct zero-byte =stderr= file remain mandatory.  Any diagnostic, a nonzero
child status, a missing PASS line, or a checked-site count other than 11 must
remain fatal.  The historical warning identity is not an accepted runtime
identity.

The first recovery run was required to use exact new top-level channel names
=runtime-recovery.stdout= and =runtime-recovery.stderr= in the retained
evidence directory.  Its caller used no-clobber creation and refused if
either path already exists.  The gate must hash and size-check the original
=runtime.stdout= and =runtime.stderr= before and after the complete matrix, so
the recovery cannot replace or mutate the first failed channels.

The gate must hard-pin the retained result path, result SHA256/size, output,
derivation, rendered-program SHA256/size, preflight snapshot, store-addition
count, and terminal realization PASS.  Its closed result must name both
checkpoints, the exact validator-owned loader identity, 11 empty-stderr sites,
and report:

#+begin_example
lowering=NOT-RUN
realization=NOT-RUN
live-action=NOT-GRANTED
#+end_example

The checkout-unavailable validator may then be rerun against the same retained
result and output.  Neither corrected validator may invoke the preflight,
lowering, build, or realization gate.

Both the source recovery gate and runtime recovery gate must fail if
=SK_P52B_D4B_ACCEPTANCE_TOKEN= is present, even if its value is empty.  The
consumed realization token is neither an input to nor authorization for this
source-only recovery.

* First published recovery and safe matcher stop

The first loader/runtime recovery was published at validator checkpoint
=99dfdeff36443aad4627fb6bb87dd6c42dcb2e87=.  It retained
=002d1ef05bab80162f6807a5cd31b98d209baa7c= as the realization and source
archive, used the existing result and output, and did not run preflight,
lowering, build, realization, confirmation, or a live action.

The no-clobber first recovery channels are retained:

| Channel | Bytes | SHA256 |
|---------+-------+--------|
| =runtime-recovery.stdout= | 0 | =e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855= |
| =runtime-recovery.stderr= | 129 | =a29ff114427bca8a4d1bb786a3b77cec9d592b3da2cb37c3d0efbf9f47a670b5= |

Its exact top-level diagnostic is:

#+begin_example
guix-system-pruning-d4b-runtime-check: FAIL: exact-production suite did not emit exactly one PASS: production-suite:orchestrator
#+end_example

The boundary and root-backend suites had already succeeded with direct
zero-byte child stderr.  This proves that the first correction removed the
historical Guile warning at both reached nested-loader sites.  The
orchestrator child also exited zero and emitted its successful frozen line:

#+begin_example
guix-system-pruning-orchestrator-check: PASS (313 checks)
#+end_example

The validator nevertheless counted zero matches because its expected
whole-line literal was the older bare
=guix-system-pruning-orchestrator-check: PASS=.  The failure is therefore a
safe harness-policy stop, not a failure of the retained artifact, its loader,
the orchestrator semantics, or empty-stderr enforcement.  The following
quantified suites had the same latent expectation risk, so the next candidate
pins all six literals together:

| Suite | Exact required PASS line |
|-------+--------------------------|
| Boundary | =guix-system-pruning-boundary-check: PASS= |
| Root backend | =guix-system-pruning-root-backend-check: PASS= |
| Orchestrator | =guix-system-pruning-orchestrator-check: PASS (313 checks)= |
| Reconciliation | =guix-system-pruning-reconciliation-check: PASS (128 checks)= |
| Phase engine | =guix-system-pruning-phase-engine-check: PASS (3172 checks phases=78 rollback-prefixes=14 rollback-restarts=204 cleanup-prefixes=8)= |
| Fixture runtime | =guix-system-pruning-fixture-runtime-check: PASS (297 checks forward-effects=71 recovery-effects=47 illegal-chains=5)= |

No second realization occurred.  Both =runtime.*= and
=runtime-recovery.*= remain immutable failed-attempt evidence.  At that
first-recovery boundary, a read-only post-stop audit confirmed both pairs'
exact identities, the intact result and artifact, no result link or artifact
GC root, and no =runtime-recovery-2.*= paths.  Its fresh 19-record
selected-live snapshot was byte-identical to the retained surfaces at
=44a31120baabf015556e42a387903153ce6b54f02624b12ab7dbc21bc80dfc11=
/ 1,455 bytes.  This is a bounded nonmutation audit, not final runtime
acceptance.

* Second published recovery and safe fixture-capability stop

The six-literal validator correction was published with clean local,
upstream, and remote equality at
=bf8040dfc3ec4438c2f229de60978792ffe0a493=.  Its exact source identities
were:

| Source | Bytes | SHA256 |
|--------+-------+--------|
| Loader checker, unchanged from =99dfdeff= | 18,049 | =6591f0292d02ae98d8267ce038a44efbe9e10f61e948e8d508f54dd554cfa030= |
| Runtime validator | 61,745 | =d4cc2ee06d572a6ac0df58d5cf0171277314f7cbb8f738df9314658e210101c2= |
| Aggregate source gate | 20,384 | =11fc5047f9402589e3f29179cf6fd797f090e16b65b47bbfd2b05b229cab263d= |

The second recovery created its own no-clobber channels:

| Channel | Bytes | SHA256 |
|---------+-------+--------|
| =runtime-recovery-2.stdout= | 0 | =e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855= |
| =runtime-recovery-2.stderr= | 115 | =fab5d2d47758443c8714f5e5f47cf21158f58dc903675060d11ed6def6462c74= |

Its exact diagnostic is:

#+begin_example
guix-system-pruning-fused: FAIL: sk-system-pruning-reconciliation ("fixture root is not an exact owned directory")
#+end_example

All six byte-frozen production suites necessarily passed with their exact two
stdout lines and empty nested-loader stderr before the validator entered the
D4a artifact fixture phase.  At its first fixture verification, the
artifact's capability guard correctly rejected the harness-created 0755 root;
the closed D4a reconciliation contract requires an exact owned 0700 root.
This was a fail-closed validator-harness topology mismatch, not an artifact
semantic failure.

The harness had one combined fixture constructor.  It supplied legacy
transaction topology to both paths: a legacy transaction sentinel and
precreated transaction/root namespaces.  The complete correction must keep
two explicit topologies:

1. D4a artifact topology has an exact owned 0700 root, the exact
   =.p52b-system-pruning-reconciliation= sentinel with 0600 mode and closed
   bytes, and absent transaction and root namespaces.
2. Legacy 98-point topology retains the transaction fixture sentinel and the
   directories required by the embedded legacy transaction engine.

The second attempt is not retried at =bf8040d= and neither of its channels may
be overwritten.  The original, first-recovery, and second-recovery channel
pairs remain failed-attempt evidence.  No second lowering, build,
realization, confirmation, result link, GC root, or live change occurred.

* Published third validator and successful D4b runtime proof

The source-only fixture-topology correction was published with clean local,
upstream, and remote equality at
=cdd4bd489313f36c325ea0841ad7586b6d152904=.  Its final source identities
are:

| Source | Bytes | SHA256 |
|--------+-------+--------|
| Loader checker, unchanged | 18,049 | =6591f0292d02ae98d8267ce038a44efbe9e10f61e948e8d508f54dd554cfa030= |
| Runtime validator | 68,082 | =dca63d3d1ab1d9a87e41d0fd8cc4990f1ac8db7c4f3a693f9e587826e9663dfe= |
| Aggregate source gate | 20,662 | =fcacf9c362d4219783df6eb642cdb616dcbdd96968f9c7a932695d6be5738409= |

The third recovery used exact no-clobber channels:

| Channel | Bytes | SHA256 |
|---------+-------+--------|
| =runtime-recovery-3.stdout= | 4,582 | =5c123e7d134d43398a0573fb19fa9668f323d0d112532c0f101c7b52b016f3a8= |
| =runtime-recovery-3.stderr= | 0 | =e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855= |

The exact 78-line stdout is tracked at
[[file:data/2026-07-20-p5.2b-d4b-runtime-recovery-3.stdout][=data/2026-07-20-p5.2b-d4b-runtime-recovery-3.stdout=]].
No repository empty file is needed for the exact zero-byte stderr.

The 4,582-byte stdout contains two closed PASS packets:

1. =p5.2b-d4b-runtime-evidence/v4= binds realization checkpoint =002d1ef=,
   validator checkpoint =cdd4bd4=, the retained result and program, all three
   failed attempts, six production suites, five D4a actions, four loader
   modes, 11 empty nested-loader stderr sites, and all =54 + 44 = 98=
   artifact interruption points.  It terminates with
   =guix-system-pruning-d4b-runtime-check: PASS=.
2. =p5.2b-d4b-checkout-independence/v1= binds the same result and program,
   records =checkout=RENAMED-AND-RESTORED=, and terminates with
   =guix-system-pruning-d4b-checkout-independence: PASS=.

The successful gate reports =authorization=NOT-GRANTED=,
=lowering=NOT-RUN=, =realization=NOT-RUN=,
=original-wrapper-regression=NOT-RUN=, and
=live-action=NOT-GRANTED=.  The checkout was restored.  All original and
failed-recovery channel pairs, the 914-byte realization result, and the exact
956,987-byte artifact remained unchanged.

A fresh selected-live comparison is byte-identical at
=44a31120baabf015556e42a387903153ce6b54f02624b12ab7dbc21bc80dfc11=
/ 1,455 bytes / 19 records.  No result link, artifact GC root, production
root, live transaction, activation, reconfiguration, collection, or reboot
was created or run.  The failed runtime attempts remain exact historical
evidence rather than being discarded or overwritten.

The D4b evidence is complete and published with clean upstream equality at
=9577e1daac3d5258222d1cbf186facba809d10fb=.  D4c.1a may now begin only as
source/schema/fixture work; the fresh read-only live-state review and
manifest-publication gate remains D4c.1b.

* Exact nonclaims

This recovery record does not claim:

- that the historical 117-byte warning is safe, classified, allowed, or
  suppressible;
- that the =99dfdeff= matcher stop is an artifact semantic failure;
- that the =bf8040d= fixture-capability stop is an artifact semantic failure;
- that =original-wrapper-regression= ran during the final runtime-v4 gate;
- a second lowering, build, realization, confirmation, substitute, graft,
  offload, result symlink, or GC root;
- a live manifest, production root, transaction, generation-link deletion,
  GRUB or =bootcfg= mutation;
- authorization for D4c, D5, garbage collection, activation,
  reconfiguration, or reboot; or
- P5.3 readiness.
